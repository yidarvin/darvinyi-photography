# Chapter critique findings (Fable-5, 28 agents)

Source of truth for the fix session. Each item is a LEAD to verify, not a verified defect. For anything marked verifyHow=browser (overlay pixel placement, live filter/widget behavior), re-verify in the preview browser by canvas-sampling BEFORE editing; several critics eyeballed coordinates on the raw image (imprecise) and may be wrong. Fix only what verification confirms.


## 0. preface
_Verdict:_ A tight, on-voice preface that does its job in under a page; the only real defects are a photo caption promising overlay labels that do not exist and a missing sequential id on the AnnotatedPhoto.
**Top fixes (ranked):**
1. Fix the AnnotatedPhoto caption to match the two labels that actually exist (or add a third label on the aperture ring, which is faintly visible at the base of the lens barrel): e.g. "Toggle the labels to find the lens and its macro and distance scale. The aperture ring, and the leaf shutter hidden inside the lens, are the parts Part I takes apart."
2. Give the AnnotatedPhoto id="0.1" and renumber the Figure to id="0.2" to match the book's sequential-id convention.
3. De-duplicate the four-part map: rewrite the Figure 0.1 caption so it teaches (reading order guidance) instead of restating the diagram, or cut the redundant lead sentence of the paragraph that follows.
4. Swap "for one" to "for instance" in the shutter sentence and reword "goes as deep as it rewards" so neither sentence forces a re-read.
**Correctness:**
- [MED][caption][static] leica-q3.jpg (AnnotatedPhoto, unnumbered): Caption says "Toggle the labels: the aperture ring, the macro and distance scale, and the leaf shutter built into the lens", but the overlay has only two labels ("28mm f/1.7 Summilux" and "macro + distance scale"). There is no aperture-ring label, and the leaf shutter is invisible inside the lens, so the caption promises annotations the overlay does not deliver.
- [MED][id][static] leica-q3.jpg (AnnotatedPhoto): The AnnotatedPhoto has no id prop. House convention numbers every image object sequentially within the chapter (see reading-light.mdx: photos 12.1-12.3, Figure 12.4, ...); here the Figure takes 0.1 and the photo is unnumbered. The photo should be id 0.1 and the Figure renumbered 0.2.
- [LOW][overlay][browser] leica-q3.jpg overlay labels: Viewed statically, both leader lines land plausibly: label 1's line ends on the SUMMILUX 1:1.7/28 engraving on the hood front, label 2's ends on the MACRO/distance ring. Pixel-precise placement at render size (label text at x40/y74 and x74/y15 clearing the leader endpoints) should be eyeballed live.
**Image density:** Right weight for frontmatter: one AnnotatedPhoto plus the signature BookMap figure, no padding. The absence of a Widget is defensible for a preface (there is no mechanism to interact with) and should not be padded to satisfy the one-widget rule. Do not add images to hit a number.
  - idea: Optionally pair the Q3 photo with (or swap in a diptych including) the Hasselblad X2D II, since the preface's whole engine is the two-camera contrast yet only one machine is pictured. (where: Paragraph 2 ("It is written around two cameras..."); why: The prose says the disagreement between the two bodies is where the lesson lives, but the reader only ever sees one of them; a second photo would show the contrast rather than assert it. Only worth doing if a properly licensed X2D II image exists; otherwise leave as is.)
**Clarity:**
- What the book assumes: "The chapters on the shutter, for one, do not stop at...": "for one" is used to mean "for example", which momentarily reads as "for one chapter" and forces a re-parse. -> Change to "The shutter chapters, for instance, do not stop at..." or drop the aside entirely: "The chapters on the shutter do not stop at..."
- What the book assumes: "Every idea starts from the beginning and goes as deep as it rewards.": "as deep as it rewards" is over-compressed; the verb has no object and the reader has to guess the intended sense (depth proportional to payoff). -> Reword to "goes as deep as the payoff justifies" or "goes exactly as deep as it is worth going."
**Verbosity:**
- Figure 0.1 caption vs the paragraph that follows it: The four-part map is stated three times in a row: the BookMap SVG labels, the caption ("the camera, then the frame, then the file, then the work..."), and the next paragraph's opening enumeration ("Part I is the machine... Part II is the eye..."). The caption is pure repetition of the diagram it sits under. -> Make the caption teach instead of restate: e.g. "Read Part I in order; after that the parts stand alone." Or keep the caption and cut "The four parts move in the order a picture does" from the following paragraph, letting it open directly with "Part I is the machine..."
- Paragraph 2: "They disagree in useful ways... and the disagreement between them is where the lesson lives.": "disagree"/"disagreement" lands twice in three sentences; the closing clause restates what "They disagree in useful ways" already established. -> Keep one. Either cut the closing clause or reword it, e.g. "and the gap between them is where the lesson lives."

## 1. the-exposure-triangle
_Verdict:_ Tight, well-taught mechanism chapter with a strong figure and widget, but its single photo's caption and alt describe a tripod light-trail shot while the actual frame is taken from inside a moving car with nothing sharp anywhere.
**Top fixes (ranked):**
1. Fix image 1.2's caption and alt to match the photo (camera moving inside a car, streetlamp trails from 2s of travel, nothing sharp), or swap in a tripod light-trail frame that actually shows a sharp road with car trails; the current alt's 'road and overpass stay sharp' is flatly false.
2. Reword 'turned the line into the third side' to 'turned the line into a triangle' in the film paragraph.
3. Cut the Cambridge in Colour table sentence in 'Equivalent exposures'; the numeric walkthrough right after it makes the same point better.
4. Add one clause in the f-number paragraph stating the number grows as the opening shrinks (and drop the imprecise 'as much glass as its front element allows').
**Correctness:**
- [HIGH][alt][static] 1.2: Alt claims 'the road and overpass stay sharp.' Viewing the file: the photo is shot through a windshield of a moving car (dashboard visible at bottom), the lane markings are smeared into streaks, roadside trees are motion-blurred, and no overpass is identifiable. Nothing in the frame is sharp. The alt also attributes the streaks to 'headlights and taillights of moving cars' when the dominant bright streaks are overhead streetlamps drawn across the sky by the camera's own motion.
- [HIGH][caption][static] 1.2: Caption says 'A two-second exposure smears the moving cars into light trails,' framing a static-camera shot. In the actual photo the camera is the moving thing: the trails are static streetlights traced by the car's travel, and the cars ahead are soft blobs, not trails. The f/13-answers-2s teaching point survives, but the described mechanism (moving cars, implied fixed camera) is wrong for this frame. Rewrite around camera motion or swap the photo.
- [LOW][widget][static] equivalent_exposure: ISO dial endpoints are labeled 'clean .. bright'. The other dials label the setting direction at both ends (wide/narrow, slow/fast); 'bright' names an output instead, and clashes with the chapter's framing that high ISO's price is noise. 'clean .. noisy' would be consistent. Hint otherwise matches widget behavior exactly.
- [LOW][overlay][browser] 1.2: Overlay placement looks grossly correct from viewing the image (vanishing-point circle at the road's convergence, '2s of travel' on the top-right streetlight streak, dashed lines tracing the lane edges), but exact registration of the circle on the convergence point is a pixel-level check.
**Image density:** Correct for a mechanism chapter. One AnnotatedPhoto plus the signature figure and widget sits inside the 1-3 target, and each teaches a distinct thing (model, trade interaction, side effect made visible). Do not add images. The only issue is that the one photo is mismatched to its stated lesson, which is a correctness problem, not a density one.
  - idea: Consider swapping light-trails.jpg for a tripod-shot light-trail frame where the road and surroundings really are sharp and only the cars smear (where: The three side effects, id 1.2; why: The intended lesson (shutter smears the moving thing while the static scene stays sharp, and a small aperture answers the slow shutter) is exactly what a tripod frame shows; the current camera-in-motion frame teaches it only after heavy recaptioning. Alternative: keep the photo and rewrite caption/alt around the car's own 2 seconds of travel, which the '2s of travel' overlay label already gestures at.)
**Clarity:**
- end of 'Equivalent exposures': 'Digital handed ISO back to you frame by frame and turned the line into the third side.': Geometrically muddled. The aperture-shutter line did not become the third side; ISO became the third side, turning the line into a triangle. -> Reword to '...and turned the line into a triangle.'
- 'One quantity, measured in stops', the f-number paragraph ending 'the full stops climb by factors of 1.4': The paragraph never states that a bigger f-number means a smaller opening and less light. A beginner can read 'the stops climb by 1.4' and assume brightness climbs with the number; the inversion is only implied two sections later. -> Add one clause, e.g. 'The number grows as the opening shrinks, so f/11 passes far less light than f/2.'
- same paragraph: 'and about as much glass as its front element allows': Hand-wavy and technically off; the f-number is set by the entrance pupil, not the front element. The clause decorates rather than teaches. -> Cut the clause; end the sentence at 'a touch wider than f/2.'
**Verbosity:**
- 'Equivalent exposures', first paragraph: 'Cambridge in Colour makes the point with a table of aperture and shutter pairs and one caption: they all result in the same exposure.': A citation dressed as prose. It restates the idea the next paragraph demonstrates in concrete numbers, and the source already sits in Sources. -> Cut the sentence; go straight from 'the total light is unchanged' to 'Photographers call these equivalent exposures...'
- caption of 1.2 (light-trails AnnotatedPhoto): Four sentences, roughly 80 words, doing four jobs (side effect, f/13 pairing, triangle reading, overlay instruction). Long even before the accuracy rewrite it needs. -> When rewriting for accuracy, trim to two or three sentences: the slow-shutter/small-aperture pairing and the overlay instruction carry the lesson.

## 2. aperture-and-depth-of-field
_Verdict:_ A tight, well-verified mechanism chapter with a correct figure, widget, and photo set, marred by one repeated numeric error: the 0.79x format advantage is about two-thirds of a stop, not "a fifth of a stop."
**Top fixes (ranked):**
1. Fix the repeated factual error in 'Why f/1.7 is not f/1.7': change "a fifth of a stop" (appears twice) to "about two thirds of a stop"; 0.79x crop = ~0.68 EV, and f/2.5 -> f/2.0 is exactly 2/3 stop.
2. Rename "entry pupil" to the standard term "entrance pupil" in the prose and Exercise 01.
3. Remove the front/behind asymmetry from the first DoF paragraph so it is taught once, in the paragraph that owns it.
4. Trim the final sentence of Photo 2.3's caption, which repeats the diffraction point made in the section prose.
**Image density:** Correct for a mechanism chapter: 2 photos + 1 figure + 1 widget, and 1-3 images is exactly right here. The two photos bracket the concept from opposite ends (85mm f/1.2 wide-open isolation tied to the 71mm physical hole; 10mm f/13 deep landscape tied to a sub-millimeter hole and the diffraction floor), so neither is redundant and nothing is missing. Do not add images.
**Clarity:**
- Why f/1.7 is not f/1.7: "worth about a fifth of a stop more depth-of-field separation" and later "undersells it by a fifth of a stop of format": Numerically wrong, and stated twice. A 0.79x crop factor is 2*log2(1/0.79) = 0.68 EV, about two-thirds of a stop; f/2.5 to f/2.0 is exactly 2/3 stop on the standard scale. The author appears to have conflated the f-number shrinking by about a fifth (0.79 = ~4/5) with a fifth of a stop. -> Replace both instances with "about two thirds of a stop" (or phrase as "the f-number drops by about a fifth, roughly two thirds of a stop"). Everything else in those paragraphs (f/1.7 -> f/1.3, f/2.5 -> f/2.0) is already consistent with two-thirds.
- The size of the hole: "the physical diameter of that opening, the entry pupil" and Exercise 01 "Work out the entry pupil": "Entry pupil" is a nonstandard term; the optics term every source (including the chapter's own Cambridge in Colour citations) uses is "entrance pupil." A reader who googles "entry pupil" gets weaker results. -> Rename to "entrance pupil" in both spots (and in the figure/widget source comments if touched).
- The plane and the zone: "is about f squared over N times c, the focal length squared divided by the f-number times the circle of confusion": Both the formula reading and its gloss are ambiguous about binding: "divided by the f-number times the circle of confusion" can parse as (f^2/N)*c. The correct form is f^2/(N*c). -> Gloss it as "the focal length squared divided by the product of the f-number and the circle of confusion."
**Verbosity:**
- The plane and the zone, first paragraph: "some in front of the plane of focus and more behind it": Pre-states the very point the next paragraph opens with ("The band is not centered on the subject. It always runs deeper behind the plane of focus than in front"), so the front/behind asymmetry is taught twice in adjacent paragraphs. -> In the first paragraph say simply "the band of distances around the plane of focus where the blur stays small enough to pass as sharp" and let the next paragraph own the asymmetry.
- Photo 2.3 caption, last sentence: "Stopped down much further, the whole frame would begin to soften.": Repeats the diffraction point the section prose just made directly above the photo; the caption's job here is the tiny-hole-buys-depth observation, which the first three sentences already do. -> Cut the last sentence (the "diffraction cost stays mild" clause already nods to the tradeoff).

## 3. shutter-speed-and-motion
_Verdict:_ A tight, correctly-built mechanism chapter whose figure, widget, and three photos all pull their weight; the only real defects are two numeric wording errors (the 900x dial-misread claim and a caption calling 1/4000 s "four thousandths of a second").
**Top fixes (ranked):**
1. Fix the dial-misread arithmetic in The duration dial: 60" vs 60 differ by a factor of thirty-six hundred (3600), not nine hundred.
2. Fix caption 3.2: replace "four thousandths of a second" with "a four-thousandth of a second" (1/4000 s is 0.25 ms, not 4 ms).
3. Cut the near-verbatim duplication between the Feel it intro prose and the widget hint; keep the instruction in one place.
4. Rewrite "The slow shutter is not incidental, it is what gives the background time to blur" to a plain statement, e.g. "The slow shutter is what gives the background time to blur."
**Correctness:**
- [MED][caption][static] 3.2: Caption says "at four thousandths of a second the image moves too little to smear." Four thousandths of a second is 4/1000 = 1/250 s; the exposure is 1/4000 s, one four-thousandth. The wording states the wrong duration by a factor of 16.
- [LOW][aitell][static] shutter-speed-and-motion.mdx panning paragraph: "The slow shutter is not incidental, it is what gives the background time to blur" is the "not A, it is B" construction, kin of the banned "it's not just A, it's B" pattern.
- [LOW][overlay][browser] 3.4: The "sharp" rect (y 43-89) extends roughly 8 points below the cyclist's wheels (which bottom out near y 81 in the photo), so the box labeled "sharp: camera swung with him" encloses a band of streaked cobbles; the label text at y 95 may also collide with the arrow line at y 93.
**Image density:** Correct for a mechanism chapter. It sits at exactly 3 images, the top of the 1-3 band, and each earns its slot with a point the schematic cannot make: 3.2 freezes chaotic fast motion, 3.3 shows the slow end used deliberately, 3.4 shows panning with an overlay separating sharp subject from streaked background. No redundancy, and nothing should be added.
**Clarity:**
- The duration dial, last sentence: "They differ by a factor of nine hundred": The math is wrong. 60" (60 s) versus 60 (1/60 s) differ by a factor of 3600, not 900. A chapter teaching dial literacy cannot misstate its own arithmetic. -> Change to "They differ by a factor of thirty-six hundred, so misreading the mark ruins the frame."
- What the shutter actually freezes, second paragraph ("The subtle half is the image speed..."): One paragraph carries the m = f/u derivation, all three levers, the cyclist example, and the freight train example. The levers get buried mid-stream and the reader hits the cyclist before digesting them. -> Break the paragraph after "Read the levers off that." so the three levers plus the two worked examples form their own paragraph; no rewriting needed, just the split.
**Verbosity:**
- Feel it, intro paragraph vs the Widget hint: The prose ("step the shutter up until the streak collapses to a point, then switch to the passing car without touching the dial") and the hint ("step the shutter until the streak collapses to a point. Change the subject without touching the dial") say the same thing nearly verbatim. -> Keep the hint as the instruction and cut the duplicated clause from the prose, letting the paragraph carry only what the hint does not: the fixed 28mm/60MP setup and the six-stop portrait-to-car observation.
- "...and it is that angular rate, speed divided by distance and scaled by the lens, that the shutter has to outrun": This clause restates the f/u law that the same paragraph just derived two sentences earlier; the cyclist example works without re-deriving it. -> End the cyclist sentence at "...they sweep a far larger angle across your frame each second." and let the freight train sentence carry the distance point.

## 4. iso-sensor-and-dynamic-range
_Verdict:_ A tight, factually careful mechanism chapter with correct images and a widget that does exactly what its hint says; the only real soft spots are one ambiguous sentence in the second-base-ISO section and some restatement of the "ISO adds no light" idea.
**Top fixes (ranked):**
1. Rewrite "Below it, at ISO 100, the deepest shadows are very slightly better" to state what is better than what, and square it with the chapter's own X2D II base of ISO 50 and with the "cleaner higher-gain mode at 200" claim (verify direction against Kasson before rewording).
2. In figure 4.2's caption, change "The floor holds still" to "The floor barely moves" so the caption stops contradicting the read-noise-falls-with-ISO mechanism the second-base-ISO section depends on.
3. Merge the doubled expose-to-the-right instruction in the last paragraph of "The second base ISO" into one sentence.
4. Cut one restatement of the ISO-adds-no-light point (drop "Every setting above base is the same capture amplified harder" and tighten the Cambridge in Colour sentence).
**Image density:** Correct for a mechanism chapter: two photos plus the one figure and one widget, and 1-3 images is the right band here. The two photos teach genuinely distinct halves of the thesis (4.1 = what high-ISO gain costs in grain, 4.3 = what base ISO buys in range), so there is no redundancy and nothing should be added.
**Clarity:**
- The second base ISO: "Below it, at ISO 100, the deepest shadows are very slightly better.": Ambiguous comparison: better than what? Under the natural reading (ISO 100 beats ISO 200) it contradicts the previous sentence, which says the sensor switches into a CLEANER higher-gain mode at 200. It also names ISO 100 when the chapter established the X2D II's base as ISO 50, so the reader wonders where 100 came from. -> State the comparison explicitly and reconcile with base ISO 50, e.g.: "Below 200 the sensor is not yet invariant: turning the dial up still buys slightly cleaner deep shadows than lifting the raw later, so a frame shot at 50 or 100 and pushed hard gives up a little to one shot at 200." (Confirm direction against Kasson's article before rewording.)
- Figure 4.2 caption: "The floor holds still.": Two sections later the ISO-invariance argument depends on read noise FALLING with ISO until the invariance point ("the read noise has stopped falling"). The caption's absolute claim quietly contradicts the mechanism the second-base-ISO section relies on. -> Soften the caption to match the prose in "Why base ISO holds the most": "The floor barely moves" instead of "The floor holds still." One word change resolves the tension.
**Verbosity:**
- "The dial that adds no light", first paragraph (the Cambridge in Colour sentence) and the widget paragraph: The point that ISO amplifies without adding light is made four times: the intro, the Cambridge in Colour paraphrase, "Every setting above base is the same capture amplified harder", and again in the widget lead-in ("amplifying a capture cannot add photons it did not have"). Two of these are pure restatement. -> Cut "Every setting above base is the same capture amplified harder." and trim the Cambridge in Colour sentence to its second half (the noise-lifting consequence), which is the only new information it carries.
- "The second base ISO", final paragraph: Expose-to-the-right is prescribed twice in one paragraph: "If you can, expose to the right at base ISO... then bring the brightness down in processing" and then "On an ISO-invariant body you can hold near base ISO, expose for the highlights, and lift the shadows several stops later...". Same instruction, two wordings. -> Merge into one sentence: introduce ETTR (with the Reichmann attribution and the square-root reason) and fold the ISO-invariant payoff into its tail clause, cutting roughly two sentences.
- Caption 4.1 (turtle): Five sentences; the third ("The grain lives where few photons landed...") and the two overlay labels say the same thing. -> Cut or compress the third sentence; the labels already point at the flat wall and the dark shell.

## 5. focus-and-autofocus
_Verdict:_ A strong, correctly-scoped mechanism chapter with an excellent figure/widget pair; its real flaws are one inverted physics sentence (the cosine error), a widget status label that contradicts the lesson, and captions that re-teach what the prose just taught.
**Top fixes (ranked):**
1. Fix the inverted cosine sentence in the focus-and-recompose section: the miss grows as the angle grows, i.e. with (1 - cos theta), not "with the cosine of the recompose angle".
2. In FocusAndAutofocusWidget.tsx, make the running status method-aware (e.g. phase shows "moving...", contrast shows "hunting...") so the widget stops labeling a one-move phase run as hunting.
3. Cut Figure 5.2's caption to ~40 words and shrink the widget lead-in paragraph to one sentence; both currently restate the section prose a second and third time.
4. Verify the 5.1 EXIF on the Commons page and reword "wide open at f/1.6" accordingly; while there, change alt "A close portrait" to match the half-body framing.
**Correctness:**
- [MED][widget][static] hunt_vs_lock: While any run is animating, the status readout shows "hunting..." (FocusAndAutofocusWidget.tsx line 115: status = running ? "hunting..." : ...), including phase-detect runs. The chapter's entire thesis is that phase detection does NOT hunt; the widget's own label contradicts the lesson during exactly the run meant to prove it.
- [MED][caption][browser] 5.1: Caption says "wide open at f/1.6". No common 85mm has a maximum aperture of f/1.6; if the lens is an f/1.4 (e.g. Sony 85mm GM) then f/1.6 is stopped down a third and "wide open" is wrong; if it is an f/1.8, the f/1.6 meta is impossible. Check the Commons file's EXIF lens model and either drop "wide open" (say "nearly wide open at f/1.6") or correct the aperture.
- [LOW][alt][static] 5.1: Alt begins "A close portrait" but the photo (viewed) is a half-body shot: hand at chin, crossed arm, beaded saree down to the hips, navel visible. "Close portrait" misdescribes the framing; the rest of the alt is accurate.
- [LOW][caption][static] 5.3: Caption says "the fur... fall[s] away" soft, but alt says "its fur textured" and, viewing the image, the fur on the near arm and face is quite sharp; only the back/rump falls off. Caption and alt mildly contradict each other on the same feature. Qualify the caption ("the far fur, the branch, and the background fall away").
- [LOW][overlay][browser] 5.1: Verified grossly correct by viewing: the two eye circles at (46,23)/(56,22) sit on the eyes, the solid plane line passes through eye level, and the downward arrow lands on the crossed arm/dress. Pixel-precise placement of the dashed near/far band lines (whether the near-band line at y~28-33 crossing the nose/lips reads as intended) is worth one live check.
**Image density:** Correct for a mechanism chapter: 2 photos + 1 figure + 1 widget, and the two photos teach genuinely distinct points (5.1 = the thin plane and where you place it; 5.3 = the where-vs-how split of subject detection). 1-3 is the right count here; do NOT add images.
**Clarity:**
- Focus and recompose section: "the size of that miss grows with the cosine of the recompose angle": Mathematically inverted. The cosine SHRINKS as the angle grows; the miss is d(1 - cos theta), so the error grows as the angle grows. As written, a careful reader concludes bigger swings mean smaller errors, the opposite of the point (and of Bob Atkins' geometry). -> Reword to: "the size of that miss grows with the recompose angle (the focused distance times one minus the cosine of the angle), so it is nothing at a few degrees and real once you have swung well off center."
- Q3 paragraph: "spread across 315 fields. That last clause matters:": "That last clause" literally points at "spread across 315 fields", but the sentence that follows is about on-sensor phase detection, i.e. the clause before it. The pointer lands on the wrong antecedent. -> Name the referent: "The phase comparison in the sensor is the part that matters: the Q3 has real on-sensor phase detection..."
- When to take the ring: "a camera with a great manual focus experience is worth using by hand when it does": "When it does" has no verb to attach to; the antecedent ("has blind spots") is two clauses back, so the sentence ends in a dangle. -> "...but it has blind spots, and when you hit one, the ring is faster than the fight." Or simply: "...worth focusing by hand when you hit one."
**Verbosity:**
- Figure 5.2 caption (110+ words): It restates the two body paragraphs immediately above it nearly clause for clause: the hidden hill, cannot-know-until-it-drops, split size gives distance / side gives direction. Triple coverage once the widget lead-in repeats it a third time. -> Cut to roughly 40 words. Keep the orienting left/right structure and the closer "One method gropes for the peak, the other reads the answer"; drop the re-explanations the prose just gave.
- Widget lead-in paragraph ("The widget lets you run each method..."): It narrates the widget's entire outcome (climbs, overshoots, settles; jumps once and locks; low light wanders) before the reader touches it, and the hint then says it again. The interaction is pre-spoiled. -> Cut to one sentence, e.g. "Run both methods on the same subject, then turn on low light and run them again; the difference in motion is the whole lesson."
- Photo 5.1 caption, last two sentences ("This is the plane you choose. The whole job of autofocus is to move the lens until that band sits exactly where you meant it to."): Restates the chapter intro's thesis inside a caption that has already made its point. -> End the caption at "...everything nearer or farther drops off."
- In-prose source name-drops ("Cambridge in Colour draws it this way", "as Cambridge in Colour and the Wikipedia article on autofocus both note", "Photography Life treats it as the standard fix"): Three-plus in-line citations in one chapter reads like a research memo, not the book's confident voice; the Sources section already carries attribution. "Bob Atkins works out the geometry" earns its place; the others do not. -> Cut the Cambridge/Wikipedia and Photography Life name-drops from prose; state the facts plainly and let Sources credit them.

## 6. metering-and-exposure-decisions
_Verdict:_ A tight, correct mechanism chapter whose figure, widget, and single photo all pull in the same direction; the only real risks are the annotated photo's overlay anchor positions and a caption doing too many jobs.
**Top fixes (ranked):**
1. Verify and fix the 6.2 overlay anchors in the browser: move the 'held white, +1 to +2 EV' label and its upward arrow off the lower-right shadow band onto clearly sunlit snow, and confirm 'shadowed slope' at (20,78) actually sits on shadow.
2. Tighten the 6.2 caption from five sentences to about three: cut the unverifiable histogram description and the 'same move the figure above draws' cross-reference.
3. Remove 'Cambridge in Colour puts the two failures side by side' from body prose and state the dove/dog example directly; the citation already lives in Sources.
4. Deduplicate the working-loop recap between the histogram section and the closing section, and reword the widget selector heading so 'gray street' (a scene the meter gets right) is not called a scene it misjudges.
**Correctness:**
- [MED][overlay][browser] 6.2: The 'held white, +1 to +2 EV' label at (74,88) and the upward arrow at x=82 (y 82 to 66) appear to sit on or beside the dark diagonal shadow band in the photo's lower right, not on sunlit snow, so the 'held white' callout may visually anchor to the one region that is not white. The 'shadowed slope' label at (20,78) also looks borderline: that area of the lower left reads as mostly sunlit slope with only scattered tree shadow.
- [LOW][alt][browser] 6.2: Alt text asserts the location is 'Kasprowy Wierch'; the Wikimedia source file is titled only 'Winter in Tatry Mountains - Poland'. If the Commons description does not name Kasprowy Wierch, the alt is inventing specificity.
- [LOW][widget][static] meter_vs_choice: The scene selector is headed 'pick a scene the meter misjudges' but includes 'gray street', whose computed target is about 0.0 EV, i.e. a scene the meter gets right. The neutral scene is a useful control, but the heading mislabels it.
**Image density:** Correct for a mechanism chapter: one Figure, one Widget, and exactly one photo (6.2), which earns its place by showing the photo-notes directive (a snowy scene the meter would misjudge, exposed the photographer's way). 1-3 is the right band and this chapter sits comfortably inside it. Do not add images.
**Clarity:**
- Why snow comes out gray: 'Cambridge in Colour puts the two failures side by side': Naming the source in body prose breaks the book's teaching voice and frames the dove/dog example as borrowed rather than taught; every other claim in the chapter is stated directly with citations reserved for the Sources section. -> State it directly: 'A white dove in a snowstorm comes out underexposed; a black dog on charcoal, overexposed. Same mistake, opposite directions.' The Cambridge in Colour citation already lives in Sources.
- Taking the wheel: 'in program it shifts the pair': 'Shifts the pair' collides with the established term 'program shift', which is a different feature that keeps total exposure constant. A reader who knows the term will read the sentence as wrong. -> Reword to 'in program it moves both dials' or 'it re-picks the pair at the new brightness'.
- How much of the frame it reads: 'Before you override the meter you get one say over its guess, which is how much of the frame it averages.': Tangled construction; 'one say over its guess' takes two reads to parse. -> 'Before you override the meter you choose how much of the frame it averages. That is the metering mode.'
**Verbosity:**
- Caption of 6.2 (snow-held-bright.jpg): Five sentences, roughly 110 words. It re-teaches the figure ('the same move the figure above draws'), describes a histogram the reader cannot see ('the tones pile toward the right of the histogram without spilling off the edge'), and narrates the overlay. Captions teach, but this one is doing prose's job. -> Cut to about three sentences: what fools the meter here, what the correction was (+1 to +2 EV), and what the overlay marks. Drop the histogram description or make it one clause ('its histogram piles right without clipping').
- 'The histogram is the honest witness' final paragraph vs 'The meter proposes, you decide': The working loop is stated twice within two paragraphs: 'Let multi-field metering propose, glance at the live histogram, and turn the compensation dial...' and then again 'dial the correction in advance and confirm it on the live histogram'. -> Keep the loop in the histogram section where it lands hardest; in the closing section keep only the scene list and the 'meter proposes, you decide' framing.

## 7. how-a-shutter-uncovers-the-frame
_Verdict:_ A strong, correctly diagram-first mechanism chapter whose one organizing question lands well; the only real defects are a self-contradictory widget readout in one reachable state, an overlay rectangle that overshoots the sensor gate, and some near-verbatim duplication around the figure and widget.
**Top fixes (ranked):**
1. Fix the widget's rolling-electronic syncLabel so it cannot contradict the dynamic readouts: make the 'flash sync' row conditional (e.g. 'only when the exposure outlasts the ~30 ms readout' when wholeFrameOpen is true at 1/30) instead of the hardcoded 'single burst won't sync'.
2. Tighten the 7.2 overlay rectangle's right edge from x=88 to ~x=80 so it hugs the sensor gate instead of spilling onto the housing (verify exact edge in the browser).
3. Deduplicate the 'Feel it' paragraph against the Widget hint; keep the interaction invitation plus one fresh observation (the 30 ms vs 4 ms traverse comparison) and drop the repeated band-pinches-into-slit walkthrough.
4. Give the EFCS cost a cause in one clause: the mechanical second curtain runs a few millimeters in front of the sensor, so at fast speeds it clips the cone of out-of-focus light.
**Correctness:**
- [MED][widget][static] uncover_the_frame: Reachable self-contradiction in the readouts: on the rolling electronic shutter at 1/30 s (d = 33 ms >= traverse 30 ms) the widget reports 'whole frame open at once? yes' and 'one flash burst lights: the whole frame', but the static syncLabel row simultaneously shows 'flash sync: single burst won't sync'. The dynamic rows are physically right (exposure outlasts readout), so the hardcoded label is the bug, and it contradicts the chapter's central yes/no lesson inside the very widget teaching it.
- [MED][overlay][browser] 7.2: The accent rectangle (x=47, width=41, so right edge at x=88) overshoots the dark sensor-gate/blade area, which ends near x~79-80 in the photo; roughly the right fifth of the outline sits on the bare magnesium housing rather than the gate it labels. Left, top, and bottom edges align well; shrink width to ~33.
**Image density:** Correct for a mechanism chapter and should not grow: one signature figure plus two photos (7.2 focal-plane module, 7.3 leaf blades mid-open) sits inside the 1-3 target, and the two photos teach genuinely distinct hardware. Real teardown photographs beat schematics here, so both earn their place. No redundancy, no gaps; 1-3 is right and this chapter is at the right end of it.
**Clarity:**
- EFCS paragraph, 'the electronic start and the mechanical finish do not quite match and out-of-focus highlights get clipped': Too compressed: the reader is given the symptom (clipped bokeh) with no visible cause. 'Do not quite match' explains nothing, so the caveat reads as trivia to memorize rather than mechanism. -> Add one concrete clause: the mechanical second curtain travels a few millimeters in front of the sensor plane, so at fast speeds its edge shaves the cone of out-of-focus light, cutting bokeh discs flat. One sentence is enough.
- 'No moving parts' section, the single paragraph starting 'What matters is how long that wave takes': One paragraph does four jobs: readout times with four camera datapoints, three advantages, shear distortion, flicker banding, and flash failure. The advantages get buried mid-block and the flicker mechanism arrives in a 40-word sentence. -> Split into two paragraphs at 'The electronic shutter buys real advantages.' and break the flicker sentence in two ('...straight lines lean. Light that flickers, which is most LED and fluorescent lighting, paints horizontal bands...').
**Verbosity:**
- Paragraph before Figure 7.1 ('The figure lays the four side by side as space-time plots...') vs the 7.1 caption: The paragraph and the caption both explain the axes and the shaded window in nearly identical words; the reader gets the reading instructions twice within a few lines. -> Cut the caption's first sentence (the axes recap) and let the body paragraph carry the how-to-read; keep the caption's band-vs-slit payoff and closing line.
- 'Feel it' paragraph vs the Widget hint: The paragraph and the hint say the same two things almost verbatim: step past 1/250 and the band pinches into a slit, switch to leaf/global and it stays a band. Also 'the flash readout flips from lighting the whole frame' is slightly off; the readout reports, the flash lights. -> Shorten the 'Feel it' paragraph to the invitation plus one observation the hint lacks (e.g. compare the rolling shutter's ~30 ms traverse against the focal-plane's ~4 ms), and reword to 'the flash readout flips from whole frame to a single band'.

## 8. rolling-shutter-and-readout
_Verdict:_ A tight, correctly diagram-first mechanism chapter with a strong figure and widget; the real risks are factual claims about the propeller photo (blade count, "every edge is sharp") and one paragraph of camera-number overload.
**Top fixes (ranked):**
1. Verify and fix the propeller blade count in the prose before photo 8.2: the aircraft is a six-blade Q400, not a "rigid three-blade propeller," and re-derive or drop the "more than one rotation" claim in the caption (check the Wikimedia file description).
2. Reconcile caption vs alt vs pixels on 8.2: remove the absolutist "Every edge is sharp" (the arcs are visibly soft and translucent in places) or rewrite the alt's "curving smears"; the two currently contradict each other.
3. Cut the two-sentence ratio paragraph after the photo and tighten "Feel it" so the turns-per-readout idea lands once instead of three times.
4. Trim the camera-number paragraph: drop the a7 IV aside and replace "fifteen to seventeen times faster" with a rounder, honest figure.
**Correctness:**
- [HIGH][caption][browser] 8.2: Prose above the photo states "The photograph below was a rigid three-blade propeller." The aircraft is a Horizon Air Bombardier Q400 (the WSU Cougars logo is visible on the nacelle), which flies a six-blade Dowty prop, and the photo shows roughly 9-10 distinct arcs, consistent with six blades, not three. The caption's dependent claim that the blades "swept through more than one rotation" is likely built on the wrong blade count and may also be wrong.
- [MED][alt][static] 8.2: Caption declares "Every edge is sharp" (prose repeats "each edge is sharp") but in the actual photo several arcs are semi-transparent, soft-edged, with visible stair-step banding on the yellow-tipped blade; the alt itself calls them "curving smears," directly contradicting the caption. Viewed statically: the absolutist sharpness claim does not match this photo. Soften caption to "the edges stay crisp where the blade is caught, and none of the shapes are real," or pick a different word than "smears" in the alt.
- [LOW][overlay][browser] 8.2: The blade-trace path (M 50 52 Q 34 48 33 42 Q 32 26 40 11) and hub circle at (50,52) look grossly plausible against the image (hub sits near 43,45; an arc does rise up-left), but the hub circle may land on the spinner's rear rather than its center, and the traced curve should sit exactly on one arc. Needs a pixel-precise check.
- [LOW][emdash][static] RollingShutterAndReadoutFigure.tsx line 51: Figure label "readout ≈ 100–165 ms" uses an en dash. Not technically the banned em dash, but the chapter prose writes "100 to 165 milliseconds" and the house style avoids dash ranges; change to "100 to 165 ms" for consistency.
**Image density:** Correct for a mechanism chapter: one signature figure, one photo, one widget. 1-3 is the target and this sits inside it. The propeller photograph genuinely earns its slot (a schematic cannot convince the way that image does), and the figure/photo/widget each teach a distinct beat: shear geometry, real-world evidence, interactive cause. Do not add images; the flicker-banding section survives fine on prose alone.
**Clarity:**
- "How fast is fast depends entirely on the sensor" paragraph: Seven camera models with milliseconds in one paragraph. The a7 IV aside ("which is actually a hair slower than the a7 III it replaced") is trivia that buries the three anchor numbers the reader needs: tens of ms conventional, ~4 ms stacked, 100-165 ms for the two cameras here. -> Cut the a7 IV clause entirely. Keep one conventional example (a7 III, 62 ms), one stacked pair (Z9/a1, ~4 ms), then the M11/Q3 and X2D II numbers. The Sources list already carries the full dataset.
- "That is fifteen to seventeen times faster": Reads as computed arithmetic, but the cited pairs span roughly 13x (Z6 51 / Z9 3.7) to 18x (a7 IV 68 / Z9 3.7), so the stated range is falsely precise. -> Say "roughly fifteen times faster" or "more than an order of magnitude faster" and stop there.
- "the camera's high-frequency, or variable, shutter mode": The double comma nesting makes the reader parse it twice; "variable" looks momentarily like it modifies "mode" on its own. -> Rephrase: "the camera's high-frequency shutter mode, sometimes labeled variable shutter, where you nudge the speed in fine fractional steps..."
**Verbosity:**
- Paragraph after the propeller photo: "The size of the warp comes down to one ratio... The widget later in this chapter is built on exactly that ratio.": The turns-per-readout ratio is stated three times: here, again in the "Feel it" paragraph ("Keep an eye on the one figure that governs it..."), and a third time in the widget hint. -> Delete this two-sentence paragraph. "Feel it" and the hint already carry the ratio at the moment the reader can act on it.
- "Feel it" paragraph: Narrates the widget beat for beat (start on leaf/global, step through presets, watch the number, slow the fan, speed it up) and then the hint repeats the same script. Roughly double coverage of one interaction. -> Trim to two or three sentences: what the widget is, the one number to watch (turns per readout), and the three-quarters-of-a-turn tear-off threshold. Let the hint hold the click-by-click.

## 9. flash-sync-and-the-leaf-advantage
_Verdict:_ A tight, well-argued mechanism chapter with a strong figure and widget; the real problems are on the single AnnotatedPhoto (overlay outline and alt both misdescribe the image) and a widget hint that promises 1/4000 sync the widget cannot deliver.
**Top fixes (ranked):**
1. Redraw the 9.2 overlay path so the traced head actually covers the head (chin near y 70, not y 44) and the shoulder flare matches his arms; verify against the live overlay in a browser.
2. Correct the 9.2 alt and caption: the shirt/shoulders are dark, not bright; credit the flash for the face and forearms, and add "at this aperture" to the closing high-speed-sync/ND claim.
3. Reconcile the widget hint with the widget: either add a 1/4000 XCD leaf mode or cut "and 1/4000 on the newest XCD lenses" from the hint so the promised interaction matches what renders.
4. Fix the sunny-16 ladder paragraph so the aperture is pinned before claiming each faster shutter stop darkens the sky; as written it follows a list of equivalent exposures where nothing darkens.
**Correctness:**
- [HIGH][overlay][browser] 9.2: The dashed head-and-shoulders trace is grossly misplaced vertically: the path's head runs y 9 to 44, but in the photo the chin sits near y 70 (eyes at ~42). The traced "chin" lands on his eyes and the traced shoulders cross his cheeks and chin; the shoulder flare (x 27-69) is also far narrower than his actual arms, which reach both bottom corners. The outline does not sit on the subject it claims to trace.
- [MED][widget][static] overpower_the_sun: Hint says the leaf shutter reaches "1/2000 on the Q3 and 1/4000 on the newest XCD lenses," but the widget's leaf mode hardcodes sync: 2000, so at 1/4000 the leaf shows a failed band with an electronic-shutter note. A reader following the hint to 1/4000 sees the opposite of what it promises. Either add an XCD 1/4000 leaf option or drop 1/4000 from the hint.
- [MED][alt][static] 9.2: Alt says "lit by a flash so his face and shirt are bright" and the caption says "face and shoulders ... read bright," but the shirt is a dark heather-gray tee that reads dark in the photo; the bright areas are the face and forearms. Both texts misdescribe the image; say face and forearms.
- [LOW][caption][static] 9.2: "...the look a focal-plane camera can reach only with high-speed sync or a neutral-density filter" omits the third route the prose itself lists (stopping down at sync speed), and this photo was very likely made exactly that way on a focal-plane Nikon. Needs the qualifier: "can reach at this aperture only with..."
- [LOW][overlay][browser] 9.2: Label "ambient sky, held down" at (46, 12) lands at or just below the top edge of his hair (hair top ~y 9 at that x), so the dot may sit on hair rather than sky. Nudging to y ~6 or moving left toward open sky (~x 25) would be safer; needs a live-overlay check.
- [LOW][widget][static] overpower_the_sun: In the above-sync state the bright band is painted across the full frame width, including the sky region (band y 80-126 overlaps sky down to 126), but a flash cannot brighten the distant sky; only the subject inside the band would light. Restricting the band's bright fill to the subject silhouette would be physically truer. Minor, since it matches the simplified strip mental model the prose teaches.
**Image density:** Correct for a mechanism chapter: one Figure, one Widget, one photo. The single photo earns its place (a real overpower-the-sun frame is the payoff the schematic promises), and 1-3 is the right count; do not pad.
  - idea: Optional only: a licensed real frame showing the black band from a flash fired past sync (dark curtain stripe across an otherwise-exposed frame), if a properly licensed one exists. (where: The sync wall section; why: The failure is the one thing the stylized widget cannot fully convey: in a real banded frame the ambient still records everywhere and only the flash is confined, which readers doing Exercise 01 will actually see. Skip it if no licensed example exists; the chapter is complete without it.)
**Clarity:**
- Two exposures in one frame: "Sunny-16 says ... f/4 at 1/2000. Each faster stop of shutter darkens the sky by a stop": The ladder just listed EQUIVALENT exposures (aperture and shutter moving together, sky brightness constant), then the next sentence claims each faster stop of shutter darkens the sky. That is only true once the aperture is pinned, which the reader is not told until the following sentence about the figure. -> Pin the aperture before the claim: "Hold f/8 and the sky is correct at 1/500; from there every faster stop of shutter pulls it down one stop, 1/1000, then 1/2000, without touching the flash-lit subject."
- The leaf shutter has no wall: "The X2D II has no shutter in its body at all": The previous section discusses both cameras' electronic shutters, which live in the body (the sensor). "No shutter in its body at all" contradicts that. -> Say "no mechanical shutter in its body at all; the blades are in the lens."
- Overpowering the sun: "the flash quits at 1/250, where that same f/8 leaves the sky a stop too bright": A stop too bright relative to what is ambiguous. It is one stop over a neutral sky but three stops over the deep sky the paragraph set as the goal. -> "...where that same f/8 leaves the sky a stop brighter than neutral, three stops brighter than the sky you asked for."
**Verbosity:**
- Intro paragraph: "up to 1/2000 on the Q3 and 1/4000 on the newest XCD lenses": The exact spec numbers appear in the intro, again in full in "The leaf shutter has no wall" (with the four lens names), again in the widget hint, and again in the callout. The intro does not need them to state the thesis. -> Trim the intro to "...so both open the entire frame together at every speed and sync a flash at all of them" and let the section carry the numbers.
- The tagline "the leaf advantage" is declared three times mid-chapter: "That is the whole of the leaf advantage" (leaf section), "which is the practical heart of the advantage" (HSS section), and the callout's "That is the leaf advantage" all land the same beat; the callout is the right place for it. -> Cut or rephrase one of the two mid-chapter declarations, e.g. end the HSS paragraph after "...none of that tax."

## 10. lenses-optics-and-perspective
_Verdict:_ A tight, physically correct mechanism chapter with a well-matched photo pair and an on-point widget; it needs one caption distance fixed, one f-stop friction between prose and figure smoothed, and two small trims.
**Top fixes (ranked):**
1. Fix caption 10.2's distance: replace 'from a butte hundreds of meters off' with kilometers (e.g. 'from a butte, the nearest ridges kilometers away') so the near-to-far-ratio argument is self-consistent with the visible scene.
2. Reconcile the sweet-spot numbers at first mention: state that two to three stops down from the Q3's f/1.7 lands near f/4 to f/5.6 (matching the figure band) while the generic full-frame rule says f/5.6 to f/8, instead of leaving the contradiction to resolve a paragraph later.
3. Cut the two inline-citation sentences ('The references are blunt about it...') in Perspective is your feet; Sources already carries both attributions.
4. De-duplicate the compression-dial moral around the widget: trim the hint to the operating instruction so the point lands once, not three times.
**Correctness:**
- [MED][caption][static] 10.2: Caption says 'Shot at 163mm from a butte hundreds of meters off'. The photo is from Steptoe Butte and the framed hills and farmstead are kilometers away, not hundreds of meters; the frame visibly spans several kilometers of receding ridges. 'Hundreds of meters' also undercuts the caption's own argument that 'every hill sits at nearly the same great distance', which requires the shooting distance to dwarf the scene depth.
- [LOW][overlay][browser] 10.1: Overlay label 'eyes: a few times farther' sits at y=25 while the eyes in the photo sit near y=32-34 (the tick mark at y=33 is on them). Not grossly wrong since the tick does the pointing, but the label floats on the forehead; worth a pixel check that the three centered labels do not collide with each other or the tick marks at rendered widths.
**Image density:** Correct for a mechanism chapter: 2 photos plus the one figure and one widget. The pair is well chosen and non-redundant, one photo per end of the distance axis (close-wide expansion via the dog, far-long compression via the Palouse), and the diagram carries the aberration/diffraction mechanism. 1-3 is right; do not pad.
  - idea: Optional only if a properly licensed matched pair exists: a BeforeAfter in 'Cropping picks the slice later' wiping between a full wide frame and that same frame cropped to a tele's coverage, proving the identical perspective the prose asserts. (where: Cropping picks the slice later; why: The crop proof is the chapter's strongest argument and is currently prose-only plus an exercise; a real same-position pair would show it in one drag. Skip it if no licensable same-tripod pair can be sourced, since the exercise already has the reader shoot their own.)
**Clarity:**
- Soft wide open, sharp in the middle: 'usually two to three stops down from wide open, around f/5.6 to f/8 on full frame': The very next element is the figure, whose sweet-spot band is labeled f/4 to f/5.6. Also, two to three stops down from the Q3's f/1.7 is f/3.4 to f/4.8, so the two halves of the prose sentence disagree with each other and with the figure; the reconciliation (fine pixels pull the wall inward) only arrives a paragraph later. -> Tie the numbers at first mention: e.g. 'two to three stops down from wide open; from the Q3's f/1.7 that lands near f/4 to f/5.6, while the classic full-frame rule of thumb says f/5.6 to f/8'. Then the figure band reads as consistent instead of contradictory.
- Intro: 'Everything else laid at the lens's feet, above all perspective, the way near and far sizes relate, comes from where you stand.': Three nested appositives between subject and verb; the reader has to re-parse to find 'comes from'. -> Split it: 'Everything else laid at the lens's feet comes from where you stand. Above all perspective, the way near and far sizes relate.'
**Verbosity:**
- Perspective is your feet: 'The references are blunt about it. Wikipedia's entry on perspective distortion states... Cambridge in Colour puts it that...': Two sentences of inline citation duplicate the Sources section, which already attributes both claims in nearly the same words. Appeal-to-authority padding in an otherwise confident chapter. -> Cut both sentences; the argument and the crop proof that follows carry the point, and Sources keeps the receipts.
- Feel it section + widget hint + widget footer: The same moral is stated three times within one screenful: prose 'You never touched a compression dial, because there is no compression dial', hint 'That is perspective, and it is your feet, not the lens', and the widget's own footer '// the subject never changed size and you never touched a compression setting'. -> Trim the hint to the operating instruction (drag distance, subject held, watch the background) and let the prose or the widget footer carry the moral once.
- Caption 10.1, last sentence: 'A wide lens did not distort the dog; standing this close did, and the wide lens only let the whole animal still fit the frame.': Four-sentence caption whose closer restates the section thesis already in prose and mirrored again by caption 10.2's 'The long lens did not press the hills together'. One of the two echoes is enough. -> End caption 10.1 after 'throws the background far off'; keep the X-did-not-do-it, distance-did construction only in 10.2.

## 11. choosing-a-lens
_Verdict:_ A tight, on-spec mechanism chapter with an excellent figure/widget pair and exactly the right single photo; the real work left is a browser check of the overlay geometry and label fit on the ultra-wide strip, plus trimming a thesis that gets restated three times.
**Top fixes (ranked):**
1. Browser-verify the 11.1 overlay on the live page: canvas-sample the five panel boundaries, confirm each nested box matches the corresponding panel's actual content (they currently measure ~7% smaller than the naive 24/f ratios), and check both labels for edge clipping and left-edge overflow on the 6.2:1 strip; nudge rects and shorten labels as needed.
2. Deduplicate the widget moral: delete 'You never moved your feet. You chose a slice, and each camera charged you in its own currency.' from the 'Feel it' paragraph (the widget footer says it verbatim), and either drop 'heavier' from the hint or add a weight readout to the X2D card.
3. Tighten caption 11.1 to three sentences and rewrite the frameline clause so the 24mm-based demo maps cleanly onto the 28mm-based Q3 ('From the 28 box inward this is the Q3's finder...'), and rename the overlay label '24mm = full frame' to '24mm = the whole frame' to avoid the sensor-format misreading.
4. Fix the wide-band boundary in 'A focal length is a job': define the reportage band as 24 to 28mm so 35mm can honestly be 'a step longer', and cut the redundant thesis sentence 'None of these is a look the glass adds.'
**Correctness:**
- [MED][overlay][browser] 11.1: The nested crop rects are centered and ordered correctly and the 28 box's top edge plausibly slices the wheel rim the way the 28mm panel does, but the 28 box measures roughly 79% of the 24mm panel's width where the naive 24/28 ratio predicts ~86% (all four boxes run ~6-8% small vs the pure focal ratios). Either the boxes were feature-calibrated to the actual step-zoom FOVs (fine) or they are drawn slightly small. Needs a pixel check of each box against the content of panels 2-5.
- [MED][overlay][browser] 11.1: The photo is an 1800x290 (6.2:1) strip, so at column width it renders ~110px tall. AnnotatedPhoto labels are fixed 0.65rem HTML centered at (x,y)%: the bottom label 'boxes = 28 / 35 / 50 / 72mm crops' (~31 mono chars, ~190px) centered at x=9.75% will overflow the image's left edge and spill past panel 1 into the gutter, and the y=4 top label likely clips the top edge. Check live and shorten/reposition the labels if so.
- [LOW][widget][static] choosing_a_lens: The hint says the X2D 'swaps to a heavier lens' and the 'Feel it' prose bills 'a swap and more weight', but the widget's X2D card shows only lens name, aperture, and 'swap glass'; weight appears nowhere in the widget. Small hint/behavior mismatch: either add a weight readout to the X2D card or drop 'heavier' from the hint.
**Image density:** Correct. This is a mechanism chapter and it sits at exactly one photo plus the signature figure and widget, which is right; the London Eye step-zoom strip is the one photograph that genuinely beats the schematic (real nested crops from a fixed spot), and the figure and widget each teach a distinct, non-overlapping angle. Do not add images.
**Clarity:**
- 'A focal length is a job', second sentence: "A wide lens, roughly 24 to 35mm... A step longer, 35mm": The wide band is defined as 24 to 35mm, then 35mm is immediately introduced as 'a step longer', which reads as leaving the band it was just placed inside. -> Define the reportage band as 24 to 28mm, then present 35mm as the long end of wide: 'At the band's long end, 35mm lets you stand back a pace and stay engaged.'
- Caption 11.1, last sentence: 'so the 35 and 50 here are the camera's crop framelines': The demo strip is 24mm-based while the Q3 is 28mm-based; the sentence blurs which boxes correspond to Q3 framelines and packs three ideas into one clause. -> Split and anchor it: 'From the 28 box inward this is the Q3's finder: the 35 and 50 boxes match its crop framelines, and the reach costs only the pixels outside the slice.'
- Overlay label on 11.1: '24mm = full frame': In a chapter that says 'full-frame-equivalent' throughout, 'full frame' here reads as the sensor format, not 'the whole uncropped view'. -> Rename the label to '24mm = the whole frame' or '24mm = uncropped view'.
**Verbosity:**
- 'Feel it' paragraph vs the widget hint vs the widget's own footer comment: The same moral (you never moved, the Q3 pays in pixels, the X2D in glass, only the X2D goes wider) is stated three times back to back; the widget footer repeats 'You never moved your feet' nearly verbatim from the body. -> Cut the last two sentences of the 'Feel it' paragraph ('You never moved your feet. You chose a slice, and each camera charged you in its own currency.') and let the widget footer carry that line.
- Thesis restatement: intro ('not a look you buy. It is a job'), end of 'A focal length is a job' ('None of these is a look the glass adds'), and the Callout ('A lens is a job, not a look'): The job-not-a-look thesis appears three times in near-identical wording; the Callout restating it is fine, three is one too many. -> Cut 'None of these is a look the glass adds.' The following sentence ('Each is the distance the focal length makes you work at...') already carries the idea.
- Caption 11.1 (5 sentences, ~90 words): Sentences four and five repeat body prose about the Q3's framelines that appears two paragraphs earlier. -> Trim to three sentences: the setup, what the overlay shows, and one clause tying it to the Q3 finder.

## 12. reading-light
_Verdict:_ A strong visually-led chapter with six distinct, well-chosen photos and a truthful figure and widget; the real fixes are a factually false "same face" caption claim, a widget hint that promises a drag the code does not support, and in-prose source citations that clutter an otherwise confident voice.
**Top fixes (ranked):**
1. Rewrite caption 12.2's opening claim: 12.1 and 12.2 are different people, so change 'The same face that read flat under front light' to 'A face that would read flat under front light' (and soften 'deep shadow' in the alt to match the photo's moderate falloff).
2. Reconcile the widget's words with its behavior: either add drag handling on the plan-view sun in src/chapters/_widgets/ReadingLightWidget.tsx, or change the hint and the preceding prose from 'Drag the key light' to 'Slide the key light' / 'swing the slider'.
3. Strip the four in-prose source callouts (three Cambridge in Colour, one 'Wikipedia's portrait-lighting references'); the facts stand alone and Sources already carries attribution.
4. Move the broad/short lighting rule out of the backlight paragraph and attach it to the side-light ladder paragraph where it logically belongs.
**Correctness:**
- [MED][caption][static] 12.2: Caption claims 'The same face that read flat under front light now reads as a solid, three-dimensional head.' It is not the same face: 12.1 is a smiling woman in a black dress, 12.2 is a gray-haired man. The continuity claim is factually false as written.
- [MED][widget][static] orbit_the_light: Hint says 'Drag the key light around the subject' and the prose above says 'Drag it from beside the camera around to directly behind,' but ReadingLightWidget.tsx has no pointer/drag handlers on the plan-view sun; the only control is a range slider. Users will try to drag the sun icon and it will not respond.
- [LOW][alt][static] 12.2: Alt says 'the far side of his face falling into deep shadow.' Viewed, the photo is a weak Rembrandt: both cheeks stay readable with only moderate falloff; nothing is in deep shadow. Caption's 'drops into shadow' oversells the same way.
- [LOW][overlay][browser] 12.2: Overlay geometry is self-consistent with a key from upper screen-left (triangle polygon under the far eye at x 53-60, terminator at x~50, background spill lower right), so it is not grossly wrong, but the triangle patch and terminator line in this low-ratio photo are subtle enough that exact placement needs a live pixel check.
- [LOW][alt][static] 12.6: Alt ('broad gentle highlights and shadows that fade slowly beneath the bunch') repeats the caption's teaching points ('Highlights are broad... the shadow under the bunch fades out slowly') instead of doing the separate describe-the-image job.
- [LOW][aitell][static] reading-light.mdx: The 'not X. It is Y' reversal appears four times ('direction is not decoration. It is the control...', 'not a failure either. It is raw material', 'Light is not a quantity you have or lack. It is a set of readable properties', plus the intro variant). Kin of the banned 'not just A, it's B' construction when this repeated; vary at least two.
**Image density:** Right count and mix for a visually-led chapter: six image objects (2 Photo, 4 AnnotatedPhoto), each teaching a genuinely distinct state of the four properties (front, side/Rembrandt, backlight/rim, hard, soft, hour). The two golden-hour frames are not redundant: 12.3 teaches direction and rim, 12.7 teaches the hour's color, and caption 12.7 explicitly distinguishes them. The gaps: contrast is the only one of the four named properties with no photograph, and blue hour is described in detail but never shown.
  - idea: One photo of a scene that outruns the sensor in hard sun (blown-white sky over a correctly exposed subject, or crushed-black shadows under held highlights), captioned as the choose-which-end-to-sacrifice decision. (where: 'Contrast is the gap you have to fit' section, which currently has zero images; why: Contrast is the only property of the four taught without a visual; the section's central claim (the scene can exceed the sensor) is exactly the kind of thing a photograph proves better than prose.)
  - idea: A small companion Photo of blue hour (cool, even, shadowless dusk street or landscape) placed near the color-temperature paragraph, ideally echoing the golden-hour landscape's framing. (where: 'The hour hands you the color and the softness' section, after the golden-hour landscape; why: Blue hour gets a full paragraph and a kelvin range but no image, and the golden/blue pair side by side is the strongest possible demonstration that the hour is a color decision.)
**Clarity:**
- end of the backlight paragraph, 'There is a side rule worth carrying...': The broad vs short lighting rule is buried at the tail of a paragraph about backlight, rim, and silhouette. It has nothing to do with backlight; it belongs to the side-light ladder discussion two paragraphs earlier, and readers scanning for it will miss it. -> Move the three broad/short sentences to the end of the lighting-ladder paragraph (the one ending 'without losing itself to shadow'), or make them a standalone short paragraph directly after photo 12.2.
- 'as Cambridge in Colour puts it' / 'As Cambridge in Colour states flatly' / 'Cambridge in Colour puts a real scene' / 'Wikipedia's portrait-lighting references place': Four in-prose authority callouts interrupt the confident house voice. The book already has a Sources section that carries the attribution; naming sources mid-sentence reads like a hedge and dates the prose. -> Drop all four attributions from the prose (keep the facts) or keep at most one. E.g. 'a sphere only looks three-dimensional when the light strikes it from a front upper side' stands on its own; Sources already cites Cambridge in Colour.
- caption 12.6, 'the same subject geometry gives the opposite edge': The subject changed from a person (12.5) to grapes, so 'the same subject geometry' is literally untrue and momentarily confusing about what stayed constant. -> Rewrite to 'the same shadow geometry gives the opposite edge' or 'and the identical ray geometry now gives the opposite edge'.
**Verbosity:**
- paragraph before the widget ('The widget below does exactly that. It swings a single key light...') and the Widget hint: The prose and the hint explain the same two-view setup twice, nearly sentence for sentence (plan view = where the light stands, front view = what the camera sees). -> Trim the prose to one sentence ('The widget below swings a single key light around a subject; drag it from beside the camera to directly behind.') and let the hint carry the two-view explanation, or vice versa.
- caption tails: 12.2 'Shape is a gift of shadow, not of light.' and 12.6 'Hard and soft are the same rule read at its two ends.': Several captions end with an aphorism restating a point the surrounding prose already makes; captions run 4 to 5 sentences each and these tails are the cuttable part. -> Cut both closing aphorisms (or keep only the 12.2 one); the teaching content of each caption survives intact.
- 'It is tempting to think this is about brightness, or about whether you clipped a diffuser onto the light. It is neither.' and later 'A diffuser works the same way and only that way...': The diffuser point is made twice in the same section, once as misconception and once as mechanism. -> Cut the diffuser clause from the first mention ('It is tempting to think this is about brightness. It is not.') and let the later mechanism sentence carry the diffuser.

## 13. composition-fundamentals
_Verdict:_ A tightly argued, well-illustrated chapter whose five photos each earn their place; the fixes are a mislocated overlay chip, a misdescribing alt/caption on the lone-tree photo, a widget claim the code does not do, and the asymmetric-balance photo the ending gestures at but never shows.
**Top fixes (ranked):**
1. Move the 13.2 'vanishing point' label chip onto the feature it names, e.g. x around 58-60, y around 34, beside the circled point (the chip renders centered with no leader line, so at (46,32) it floats in empty sky).
2. Rewrite photo 13.4's alt and caption to match the frame: a lone tree in shallow water beyond a broad bare sand flat under a hazy sky, and teach that the sand, water, and sky together are the negative space (currently both claim the frame is almost all water).
3. Align the widget language with its behavior: change the prose and hint from 'the nearest power point lights up' as you slide, to lighting up when the subject lands on a thirds crossing; optionally note f works once the subject is selected.
4. Add one asymmetric-balance photograph after 13.6 (large quiet mass near center vs small bright accent far out) and fix caption 13.6's dangling 'livelier asymmetry above' reference; also cut the post-figure paragraph's repeat of the figure caption's thirds payoff.
**Correctness:**
- [MED][overlay][static] 13.2: The 'vanishing point' label chip is centered at (46,32) but the vanishing point the overlay circles sits at (67.5,37.5); AnnotatedPhoto renders label chips as centered text with no leader line, so the chip floats in empty sky roughly a fifth of the frame left of the feature it names. House rule: labels sit ON the feature.
- [MED][alt][static] 13.4: Alt says the tree stands in 'a wide expanse of shallow turquoise water that fills almost the entire frame' and the caption says everything outside the box is 'quiet water and sky', but roughly the bottom half of the photo is bright wet sand flats, not water. Both misdescribe the frame; the sand is the largest single area in it.
- [MED][widget][static] place_the_subject: The pre-widget prose ('Slide it off the center and the nearest power point lights up') and the hint ('Watch the nearest power point light up') claim the nearest point lights as you move, but the code only activates a point when the subject is within 0.06 of that intersection on both axes; sliding off center generally lights nothing.
- [LOW][caption][static] 13.6: Caption ends 'the opposite of the livelier asymmetry above', but no image above shows asymmetry; the referent is a prose paragraph, so the caption points the reader at a picture that does not exist.
- [LOW][widget][browser] place_the_subject: Hint says 'press f' to flip the gaze, but the f handler is on the subject group (tabIndex 0), not the document; before the subject has keyboard focus, pressing f does nothing.
- [LOW][aitell][static] composition-fundamentals.mdx: Intro: 'It is not a layer of polish added after the picture is made. It is the control that sets...' is the not-A-but-B construction the house style bans; borderline because both halves are concrete, but it is the pattern.
**Image density:** Right count and right mix for a visually-led chapter: five AnnotatedPhotos (thirds, leading lines, frame-within-frame, negative space, symmetry), each teaching a distinct device, plus the figure and widget. No two photos are redundant. One real gap: asymmetric balance, which the chapter calls 'the balance most photographs are reaching for' and which caption 13.6 explicitly contrasts against, exists only as the schematic figure, never as a photograph. Lead room needs no photo; the widget owns it.
  - idea: Add one photo of asymmetric balance: a large quiet mass near center answered by a small bright accent far out (e.g. a big dark headland with one small lit boat toward the opposite edge), placed after the 13.6 symmetry photo as its counterpart. (where: Balance is the sum of all of it, after photo 13.6; why: It is the chapter's climactic claim and the one device argued only in schematic; it would also give caption 13.6's 'livelier asymmetry' contrast something real to point to.)
**Clarity:**
- The eye is not neutral: 'in Cambridge in Colour's phrase, prioritized based on interest': An unquoted quote plus a source name dropped into running prose; the reader stumbles over whose words these are and why the citation is inline when every other source lives in the Sources list. -> Paraphrase and drop the inline attribution: 'the detailed scene you feel you saw was assembled by interest, one fixation at a time.' Same move for 'Cambridge in Colour puts it plainly, that it is just a rough guideline' in the thirds section: 'it is a rough guideline, nothing more.'
- Off center is where the frame wakes up: 'it does not supply a general law but universalises a particular': A raw 1845 quotation with archaic phrasing ('universalises a particular') left unglossed; most readers will not parse it on the first pass. -> Gloss or paraphrase: 'George Field objected in 1845 that the rule turns one pleasing case into a false general law, and that following it blindly yields uniform, monotonous pictures.'
**Verbosity:**
- Balance is the sum of all of it: figure 13.5 caption vs the paragraph that follows it: The lever law is stated three times in a row: the section's first paragraph ('weight times its distance from the center'), the figure caption ('weight times arm... why a subject on a third does not tip the frame: the open space opposite carries the far arm'), and the next paragraph ('It is why a subject on a third does not tip the picture over: the open space... balancing'). The caption and the paragraph are nearly verbatim duplicates. -> Cut the first two sentences of the post-figure paragraph and open it at 'Symmetry, the same weight mirrored on both halves...'. The figure caption already carries the thirds payoff.
- Photo 13.1 caption, last sentence: 'That small, resolvable imbalance is what makes an off-center frame feel alive where a centered one sits dead' restates the body paragraph two inches above it ('you introduce a small imbalance the eye wants to resolve... you make the picture move'). -> End the caption at '...then has the whole sky to wander.' The mechanism is already taught in the prose.

## 14. advanced-composition
_Verdict:_ A strong, well-imaged visually-led chapter that hits its density target with five distinct teaching photos; its only real defects are two confident factual naming claims (Leonardo's "perspective of disappearance" and the "real logarithmic spiral") that teach the wrong fact.
**Top fixes (ranked):**
1. Fix the Leonardo naming in the depth prose and caption 14.6: the blue/pale shift is his "aerial perspective"; keep "perspective of disappearance" only for the loss-of-detail observation. The Sources entry already has it right.
2. Reword caption 14.3 to drop "This is a real logarithmic spiral" (the projected helix is not one); say "a real continuous spiral, which is more than most golden-spiral overlays can claim" to keep the jab without the false claim.
3. Tighten caption 14.2: cut the "Two cues fire at once..." sentence that duplicates the prose immediately above, and browser-verify that the four rhythm ticks actually land on columns (left ticks look like they hit the brick pier).
4. Unify colour to color in the group_the_frame widget hint and readout, and either explain or cut the "began with a train ride" hook that the gestalt section never explains.
**Correctness:**
- [HIGH][caption][static] 14.6: Prose and caption misattribute Leonardo's term. The blueing/paling of distant objects is what Leonardo called aerial perspective (prospettiva aerea); the "perspective of disappearance" (prospettiva de' perdimenti) was his separate name for distant objects losing distinctness of outline and detail. The prose says "He called it the perspective of disappearance" about the color effect, and caption 14.6 repeats "exactly Leonardo's perspective of disappearance" for the color shift. The chapter's own Sources entry contradicts this, crediting Leonardo with "naming aerial perspective."
- [MED][caption][static] 14.3: "This is a real logarithmic spiral" is almost certainly false. The Momo staircase is a double helix of roughly constant radius; the looking-up view is a perspective projection of that helix, which yields a hyperbolic-type spiral (radius shrinking toward the vanishing center), not a logarithmic one. Ironic in the very paragraph-cluster that debunks golden-spiral overlays for exactly this kind of loose claim. Reword to "a real continuous spiral" or drop the sentence.
- [MED][overlay][browser] 14.2: The four tick marks (x=8, 16 left; x=80, 88 right, y 55-70) are meant to "fall on that repeat," i.e. on successive columns, but in the image the far-left region around x=8 is the large brick corner pier, not a marble column, and the prominent right-side columns sit nearer x~74 and x~86, so the ticks plausibly straddle the columns rather than land on them. Needs a pixel check against the rendered overlay.
- [LOW][widget][static] group_the_frame: British spelling "colour" in the reader-visible widget hint ("two colour families") and in the widget readout notes, while chapter prose uses American "color" ("match in color or shape or size"). Inconsistent within one page.
**Image density:** Right for a visually-led chapter: five AnnotatedPhotos plus the Figure and Widget, and each photo teaches a genuinely distinct principle (gestalt grouping, repetition/rhythm, spiral geometry, tension, aerial-perspective depth). No two images are redundant. Two named concepts get full prose treatment but no photograph, and the mix is a monoculture of AnnotatedPhoto with no plain Photo or BeforeAfter.
  - idea: Add an S-curve photograph (river, winding road, or staircase in elevation) as a plain Photo (where: 'A frame is a path, not a point', beside the Hogarth line-of-beauty paragraph; why: The serpentine line gets a full paragraph of history but the section's only photo is the spiral staircase, which shows a coil, not the S-curve the text spends its time on; a plain Photo would also break the all-AnnotatedPhoto monoculture.)
  - idea: Add a figure-ground reversal photo (a strong silhouette or shadow that flips between subject and shape) (where: 'The mind insists on order', near the Rubin vase sentence; why: Figure-ground is named as the machinery underneath every other cue and Rubin's vase is described in prose, but the chapter never shows it photographically; a silhouette that flips would carry that point better than words.)
**Clarity:**
- Opening of 'The mind insists on order': "Gestalt psychology began with a train ride.": The train-ride hook is never cashed. The next sentence jumps to the 1912 apparent-motion study without saying what the train had to do with anything, so the reader is left holding a dangling anecdote. -> Either spend half a sentence on it (Wertheimer noticed the flashing-light illusion from a train window, got off at Frankfurt, and bought a toy stroboscope to test it) or cut the hook and open with the 1912 study.
- Geometry section: "a pleasing chase along what he called the continuity of variety": The phrase is presented as Hogarth's own coinage ("what he called"). Hogarth's famous wording is the serpentine line leading the eye "a wanton kind of chace"; "the continuity of variety" is not a phrase I can place in the Analysis of Beauty, so the direct attribution is risky. -> Verify the phrase against the Analysis of Beauty; if it is not verbatim, soften to "praising its continual variety" without the "what he called" attribution.
**Verbosity:**
- Caption of 14.2 (rhythm-colonnade): "Two cues fire at once: similarity, because every column matches its neighbors, and good continuation, because they line up along one receding run.": This sentence restates, almost word for word, the prose paragraph sitting directly above the photo ("grouping by similarity because they match and by good continuation because they line up"). The reader gets the identical lesson twice within three inches of scroll, and the caption is the longest in the chapter at five sentences. -> Cut the "Two cues fire at once" sentence from the caption and keep the caption's unique payload: the ticks marking the repeat and the observation that a column out of step would seize the eye.
- Widget hint for group_the_frame: The hint pre-narrates all four outcomes (three clusters, two colour families, boundary overrides, connection beats all), which the widget's own per-cue readout then repeats verbatim as the reader clicks. The hint spoils the interaction and duplicates in-widget text. -> Shorten the hint to the setup only: "Switch the grouping cue and watch the same twenty dots reorganize. The dots never move; only the cue you give the eye does."

## 15. color-science-for-photographers
_Verdict:_ A strong, accurate, genuinely visually-led chapter whose five photos each teach a distinct relationship; the remaining debts are a wordless white-balance section, a duplicated scheme rundown between figure caption and prose, and two overlay circles that need pixel-level verification.
**Top fixes (ranked):**
1. Add a BeforeAfter (global temperature afterFilter, honestly captioned as a global white-balance move) to the "Warm, cool, and the choice of white" section, the only image-free stretch in a visually-led chapter and the direct visual for Exercise 04.
2. Browser-verify and nudge the 15.4 triad overlay circles: the "blue" circle appears to sit on the white/red-orange bow band instead of the blue hull, and the "red-orange" circle may sit on blue; also shift the 15.3 "yellow" label toward the truly yellow bottom-left of the gradient.
3. Deduplicate the four-scheme rundown: trim Figure 15.1's caption so the Itten paragraph is the one place that describes complementary/analogous/split/triadic and makes the not-a-recipe point.
4. Fix two small factual slips: date the von Kries rule to 1902 (the chapter's own source) instead of "the nineteenth century", and restate the L:M cone-ratio variation as running from roughly even to strongly L-dominant, plus unify the widget's "colour" strings to American "color".
**Correctness:**
- [MED][overlay][browser] 15.4: The "blue" circle at (62,60) appears to land on the center boat's white-and-red-orange bow band rather than the blue hull (the blue hull reads lower, roughly y 65-80 at that x), and the "red-orange" circle at (52,68) may sit on or below the band on the blue hull; the two circles look shifted or effectively swapped relative to their hues.
- [LOW][overlay][browser] 15.3: The "yellow" label at (62,90) sits where the gradient reads orange-to-red at that horizontal position; the genuinely golden-yellow region is the bottom-left corner (roughly x 0-30). Label may need to move left or down-left.
- [LOW][widget][static] simultaneous_contrast: User-visible widget strings use British spelling "colour" twice ("the colour was never in the square", "...change the colour you see") while the chapter prose and the widget hint use American "color"; inconsistent user-facing spelling within one page.
**Image density:** Right at target and well mixed for a visually-led chapter: 5 photo objects (3 AnnotatedPhoto, 1 AnnotatedPhoto for depth, 1 Photo) plus the figure and widget, and each photo teaches a distinct point (complementary contrast, analogous run, triadic balance, warm-advances depth, near-monochrome restraint). 15.2 and 15.5 share a warm-orange-against-blue palette but teach different lessons (contrast vs depth cue), so they are not redundant. The one real gap is the white-balance section, which is the only image-free stretch.
  - idea: Add a BeforeAfter to "Warm, cool, and the choice of white": one frame with warmth in the light, after side a global temperature/tint afterFilter labeled as the neutralized document against the warm memory. Keep the caption honest that it is a global white-balance move (no locality claim, so no afterClip needed). (where: Warm, cool, and the choice of white; why: That section carries color constancy, the dress, and WB-as-mood with zero images, and it directly rehearses Exercise 04; a draggable warm-vs-neutral wipe is the single most teachable visual in the chapter that is currently missing.)
  - idea: A photo of one dominant hue with a single small accent (the red-umbrella-in-a-grey-street type frame), placed just before 15.6. (where: One color, spent well; why: The prose names "one dominant hue with a single small accent" as the strong strategy, but 15.6 only shows the full near-monochrome extreme; the accent case is asserted, never shown.)
**Clarity:**
- Color is something the eye does: "with counts running anywhere from a small majority of L to nearly even": Understates and slightly misstates the documented L:M cone variation, which runs from roughly even to strongly L-dominant (multiples of M, not a small majority); as written it sounds like L never dominates by much. -> Rewrite as "with L outnumbering M anywhere from roughly even to several times over" or drop the range entirely and keep only "varies enormously from person to person".
- Warm, cool, and the choice of white: "the von Kries rule from the nineteenth century": The chapter's own cited source (Wikipedia, Chromatic adaptation) dates the von Kries transform to 1902, so "nineteenth century" contradicts the source list two screens down. -> Say "the von Kries rule, formalized in 1902" or drop the date.
**Verbosity:**
- Figure 15.1 caption and the Johannes Itten paragraph immediately after it: The caption lists all four schemes shape by shape (diameter, short arc, triangles) and closes with "not rules... naming them lets you build one on purpose"; the very next paragraph re-describes the same four schemes and re-makes the not-a-recipe point ("None of this is a recipe for a good photograph. It is a vocabulary"). The reader gets the identical rundown twice within a few lines. -> Trim the caption to the hero relationship plus one line ("the row below shows the common schemes as the shapes they are") and let the Itten paragraph carry the scheme-by-scheme detail and the not-a-recipe point once.
- Caption 15.4 (fishing boats): Says the balance idea twice: "charged yet balanced rather than dominated by one loud pair" and then "Because each color is answered by two others, no single hue wins." -> Cut one of the two balance sentences; the caption still teaches with three sentences.

## 16. black-and-white-seeing
_Verdict:_ A strong, correctly-sourced visually-led chapter with verified math and well-matched photos; its two real flaws are a self-contradicting claim in the red-barn caption and the red/green collapse example being narrated three separate times.
**Top fixes (ranked):**
1. Fix the 16.5 caption: replace "the barn wall holds its tone" with language matching the matrix's actual effect (the barn lifts to a light grey while the sky drops), removing the internal contradiction with "records bright."
2. De-duplicate the red/green collapse narration: trim the paragraph after Figure 16.1 to the naming of the value collapse, and cut the figure caption's final two thesis-restating sentences.
3. Verify the Leica Q3 display-button / "live tonal readout" claim against the Q3 manual and either name the real feature or reduce the sentence to the verified monochrome-Look-in-EVF fact.
4. Add a low-key photograph to complete the key triad the prose teaches (high-key 16.6 and full-scale 16.4 are shown; low-key is only described).
**Correctness:**
- [MED][caption][static] 16.5: Caption says "the barn wall holds its tone" under the red filter, but the svgFilter matrix in the file (1.3R + 0.1G - 0.4B) lifts the barn dramatically: a barn red of about (155,45,60) computes to roughly 71 percent grey versus about 27 percent under a plain desaturate. The same sentence even says red "records bright," contradicting "holds." The barn lightens; it does not hold.
- [LOW][overlay][browser] 16.4: The solid accent path tracing the river ends its arrowhead at (72,67); in the viewed image the river's upper-right stretch sits nearer 55 to 62 percent down, so the arrow tip may land on the trees just below the water rather than on it. The three text labels (sky, snow, river) all sit correctly on their features.
**Image density:** Right count and a good mix for a visually-led chapter: 5 image objects (3 BeforeAfter, 1 AnnotatedPhoto, 1 Photo) plus the figure and widget, and each teaches a distinct point (collapse, tonal survivor, full-scale placement, filter previsualization, high key). Mild motif overlap: the darkened-sky lesson appears in 16.4, 16.5, and the widget, but each serves a different job (placement, real-photo proof, interactive model), so none should be cut. One genuine gap: the prose names a three-way choice of key (high, low, full-scale) and shows only two.
  - idea: Add a low-key Photo (a figure or object emerging from near-black, e.g. a Salgado-style dark frame or a licensed low-key still life) beside or after 16.6. (where: What is left when the color goes; why: The text explicitly teaches the triad: high-key (16.6), full-scale (16.4), low-key (described only in words). The missing third leg is the one readers find hardest to previsualize.)
  - idea: Optional: a mixed-color-temperature interior shown as a BeforeAfter wipe to grey. (where: First paragraph of "What is left when the color goes"; why: The warm-lamp/cool-window/green-fixture quarrel that mono settles is a vivid, named scenario left unillustrated; only add it if a genuinely good licensed frame exists, since the chapter already meets target.)
**Clarity:**
- See the grey before the shutter: "The display button cycles the finder from color to monochrome to monochrome with a live tonal readout.": "Live tonal readout" is unexplained jargon (zebras? histogram? clipping warning?), and the specific display-button cycling behavior is an unverified hardware claim about the reader's own camera; the cited Overgaard source is a menu walkthrough, not obviously this. -> Either name the actual feature from the Q3 manual (e.g. "monochrome with the live histogram") or cut the sentence and keep only the verified claim: a monochrome Leica Look previews in the finder while the DNG stays full color.
- A filter redraws the tones: "A blue filter does the reverse and is used mostly to add the soft haze of distance.": Compressed to the point of oddness: haze is usually a defect, so "add the soft haze" reads as a typo for cutting it. The mechanism (blue emphasizes atmospheric scattering, exaggerating aerial perspective) is skipped. -> One more clause: "A blue filter does the reverse, emphasizing atmospheric haze so distance reads as depth."
- Zone System paragraph: "written in Roman numerals so they would not be confused with anything else": "Anything else" is vague; the reader cannot tell what the numerals avoid colliding with. -> Say it: "written in Roman numerals so they would not be confused with exposure settings or f-numbers."
**Verbosity:**
- The value collapse, paragraph after Figure 16.1: "A red flower against green leaves is one of the loudest color relationships there is... In color the subject shouts. Desaturate it and it disappears.": This is the third telling of the same red/green example: the intro paragraph narrates the berry-and-leaf collapse, this paragraph re-narrates it, and the 16.2 caption narrates it a third time over the actual berries. Exercise 01 then assigns it a fourth time. -> Trim this paragraph to the naming sentence ("This is the value collapse... High color contrast is not the same as high tonal contrast.") and let the 16.2 photo caption carry the flower/leaf specifics.
- Figure 16.1 caption, final two sentences: "The lesson under the whole chapter is that color contrast is not tonal contrast...": The caption restates the thesis that the figure itself prints ("// color contrast is not tonal contrast") and that the very next prose paragraph repeats again; the thesis appears four times within one screen. -> End the caption after "79 percent against 34 percent." The figure's own annotation and the following paragraph already state the lesson.

## 17. the-digital-negative
_Verdict:_ A tight, well-argued mechanism chapter whose figure and widget genuinely prove the thesis; the real faults are a "the panel below is the whole develop" claim contradicted by the photo's own corrected verticals, and a photo alt/caption that misdescribes the bronze statue and the look of the undeveloped half.
**Top fixes (ranked):**
1. Resolve the "whole develop" contradiction around LightroomPanel 17.3: drop "The panel below is the whole develop" (the verticals correction in caption 17.2 is a Transform move, and the crop visibly changes) and soften the caption so the sliders read as a representative develop of this negative, not the literal values behind someone else's Wikimedia edit.
2. Fix Photo 17.2's alt and caption materials: bronze statue on a stone pedestal, recovery chiefly in the statue, and describe the left half as dark and contrasty rather than "flat, dull."
3. Halve the Figure 17.1 caption; it re-lists the five pipeline stages the paragraph above just named.
4. Reword "The widget makes the two behave side by side" to a parseable sentence, e.g. "The widget develops the two side by side."
**Correctness:**
- [HIGH][figure][static] 17.3: The prose says "It was a handful of Basic-panel moves on the one negative... The panel below is the whole develop," but caption 17.2 credits "the leaning verticals corrected" and the right half is visibly re-cropped (the lamp post at the left edge is gone). Perspective correction is not a Basic-panel move, so the panel cannot be the whole develop. Additionally, the slider values are invented for a third-party Wikimedia edit (Norbert Nagel's) yet the caption asserts they are "The develop that took the flat raw on the left to the finished frame on the right," a fabricated-precision claim.
- [MED][alt][static] 17.2: Alt calls it "a stone war-memorial statue"; the statue is dark bronze (a winged figure with an eagle) standing on a stone pedestal. The alt also attributes the recovered detail to "the shadowed stone" when the dominant recovery is the bronze statue going from silhouette to full detail.
- [MED][caption][static] 17.2: Caption says the left half is the raw "as it opens with no interpretation: flat, dull." The visible left half is dark and contrasty with a deeply saturated blue sky; it reads underexposed, not flat or dull. "Shadows lifted out of the stone" also misplaces the recovery, which is chiefly in the bronze statue.
- [LOW][widget][browser] latitude_lab: Hint says "Drag the develop push up," but the widget initializes at 70%, so only 30% of travel remains and the as-shot-to-broken arc the hint describes is best seen by dragging down first. Minor hint/state mismatch.
**Image density:** Correct for a mechanism/workflow chapter. One Photo plus one LightroomPanel riding alongside the signature figure and widget is exactly the diagram-first mix this kind calls for; 1-3 is right and this sits at the low end deliberately. Nothing is redundant: the widget proves the levels argument synthetically and the photo proves it on a real file. Do not add images; a real photo of a Bayer mosaic would not beat the prose, and the demosaic point needs no picture.
**Clarity:**
- "The widget makes the two behave side by side" (end of the levels paragraph, before the widget): Garbled phrasing; "makes the two behave side by side" is not a sentence a reader can parse on first pass. -> Rewrite as "The widget develops the two side by side: one scene from a deep raw negative and from the eight-bit JPEG the camera would have baked, then push."
- Caption 17.3: "A warmer as-shot white balance": Ambiguous: is 5,600 K the as-shot value, or a value warmed past as-shot? The two readings contradict each other and the chapter just taught that WB is a changeable tag. -> Say it plainly: "White balance nudged warmer than the as-shot tag" or "the as-shot 5,600 K kept", whichever the panel intends.
**Verbosity:**
- Figure 17.1 caption: At ~90 words it restates the section directly above it, including the same five pipeline stages the prose just listed (demosaic, white balance, tone curve, color, sharpening/NR), then re-explains both taps. -> Cut to the structural claim only: one pipeline tapped at two ends, raw saves the input at full bit depth, JPEG saves the flattened output. Keep the closing line "Raw keeps the negative; JPEG keeps a performance of it." Roughly halves it.
- Paragraph after Photo 17.2 ("Nothing above was magic..."): The develop moves are narrated three times in a row: this paragraph, caption 17.2 ("shadows lifted out of the stone, highlights held in the sky"), and caption 17.3 (the same moves again, slider by slider). -> Trim the paragraph to its point (one negative, no second frame, a handful of reversible moves) and let caption 17.3 carry the specific moves. Delete the clause "pull the harsh midday highlights down off the stone and the sky, lift the shadows back out of the shaded stone, and set the white and black points so the tones fill the range."

## 18. ingest-and-catalog
_Verdict:_ A correctly diagram-first, tightly written workflow chapter; the real defects are a widget hint the widget's own logic disproves, a self-contradictory widget footer message, and a figure caption that contradicts the prose about what deleting the catalog loses.
**Top fixes (ranked):**
1. Fix the resilience_lab widget hint and footer together: replace the hint's false "Only three copies... survives all three" claim with what the code shows (3-2-1 is the setup that survives all three with a copy to spare), and add a third footer branch in IngestAndCatalogWidget.tsx for allSurvive && !rule321 so it stops referencing a disaster "still marked lost" when none is.
2. Rewrite the end of Figure 18.1's caption: "Delete it and every negative survives; what is lost is the layer above, the ratings, keywords, and edits, and the arrows tying them to the files."
3. Correct 18.3's alt to describe the sheet accurately: a man in glasses across the top row, a woman in the remaining rows.
4. Reword "then run the three disasters" to "then check it against the three disasters" and trim the Callout's third restatement of the 3-2-1 disaster mapping.
**Correctness:**
- [HIGH][widget][static] resilience_lab: Hint's last sentence ("Only three copies on two kinds of media with one of them offsite survives all three") is false to the widget's own logic: toggling just working drive + cloud backup (2 copies) survives all three disasters (>=2 copies, one offsite, one non-disk medium) while the widget reports 3-2-1 not yet met. The reader disproves the hint in two clicks.
- [MED][widget][static] src/chapters/_widgets/IngestAndCatalogWidget.tsx: Footer message else-branch (lines 176-182) prints "the disaster still marked lost is the frame you would lose today" whenever rule321 && allSurvive is false; in the working+cloud state it renders "3 of 3 disasters survived · 3-2-1 not yet met. the disaster still marked lost..." with no disaster marked lost. Self-contradictory copy; needs a third branch for allSurvive && !rule321 (e.g. explain the missing margin of a third copy).
- [MED][caption][static] 18.1: Caption ends "Delete it and every negative survives; the dashed arrows are all that is lost", but the caption itself says the catalog cards hold ratings, keywords, and develop edits, and the prose says "You would lose your edits and your organization." Deleting the catalog loses the whole upper layer, not just the arrows.
- [LOW][alt][static] 18.3: Alt says "four rows of small portrait frames of a woman", but the top row is four sideways frames of a man in glasses plus one of the woman (viewed the image). Alt should describe both subjects.
- [LOW][overlay][browser] 18.3: The keeper circle (cx 53, cy 75, r 6.5) appears to straddle the boundary between the large smiling frame (18) and the darker hand-in-hair frame (19) in the bottom row; the obvious keeper reading is the smiling frame centered near x 43. Confirm the circle rings one unambiguous frame in the live render and nudge cx if it splits two.
**Image density:** Correct for a mechanism/workflow chapter: one photo (the annotated contact sheet) plus the signature figure, the signature widget, and a well-used LightroomPanel. The contact sheet genuinely beats a schematic for teaching the cull, and 1 photo sits properly inside the 1-3 target. Do not add images.
**Clarity:**
- "The widget lets you build a setup and then try to break it. Choose where the copies of one negative live, then run the three disasters...": The widget has no run action; the three disaster rows update live as you toggle copies. "Run the three disasters" promises an interaction that does not exist. -> Reword to match the widget: "Choose where the copies of one negative live, then check it against the three disasters that actually lose pictures, and watch which ones your choice survives."
- "This layer above the files is also where your edits live, and it works the same way the negative does.": "It works the same way the negative does" is ambiguous (a layer working like a file?), and the next sentence states the actual point anyway. -> Cut the clause and let the next sentence carry it: "This layer above the files is also where your edits live. Developing a raw never rewrites the raw."
- "a four-digit counter... something like DSC00001": The example filename has five digits immediately after the text says four-digit counter (DSC00001 is real Sony naming, but the juxtaposition reads like a typo). -> Use a four-digit example that matches the claim, e.g. "something like IMG_0001" or "DSC_0001".
**Verbosity:**
- Callout: Five long sentences restating the whole chapter; the 3-2-1 clause ("because that is what survives a dead drive, a shared failure, and a house fire in turn") is now the third full statement of the same mapping (body, widget hint, callout). -> Compress to three sentences; drop the disaster enumeration and the final "Do the boring jobs once" flourish or keep only one of them.
- Exercise 01: "such as Hedge (OffShoot) or rsync with its checksum option": Repeats the exact tool names and the rsync checksum detail already given in the body paragraph on verification. -> Trim to "use a tool that checksums the copy instead of dragging files across in the Finder"; the body already names the tools.
- "Two consequences follow, and both are the reason to understand this at all.": The second clause is throat-clearing; the consequences justify themselves in the next two sentences. -> Cut to "Two consequences follow."

## 19. order-of-operations
_Verdict:_ A solid, on-spec mechanism chapter with an accurate widget and honest single BeforeAfter; its real flaws are one mislabeled figure bracket that contradicts the prose's use of "frame", one muddled double-metaphor paragraph, and a few small repetitions.
**Top fixes (ranked):**
1. Rename the figure's second phase bracket from "shape the frame" to "shape the look" in both OrderOfOperationsFigure.tsx (PHASES) and the 19.1 caption, so "frame" keeps meaning geometry/crop everywhere in the chapter.
2. Rewrite the two-rules paragraph in "A stack, not a rack of sliders" to drop the inward/outward double metaphor: state "global before local" and "fix the frame's shape before judging tone inside it" plainly, then name the convention once.
3. Change the figure's noise-reduction note to "assumes: tone and color are set"; leave the size dependency on the sharpen & export rung where the prose puts it.
4. Tighten the repetitions: cut the standalone "Denoise first, then sharpen.", cut or fold the retuning-payoff sentence at the end of the go-local paragraph, and fix the widget's "sized to size" blurb typo.
**Correctness:**
- [MED][figure][static] 19.1: The figure's phase bracket "shape the frame" spans contrast & curve and color & grade (PHASES in src/chapters/_figures/OrderOfOperationsFigure.tsx), but everywhere else in the chapter "frame" means geometry and crop, which the figure files under "set the base". The 19.1 caption repeats the mislabel. A reader mapping prose to figure will look for geometry under that bracket and not find it.
- [MED][figure][static] 19.1: The noise-reduction rung's note reads "assumes: the final crop and size", but the prose ties NR's placement to tone and color being set (contrast pushes make noise worse) and assigns the size dependency to output sharpening, which the sharpen rung already carries ("sized to output"). The figure contradicts the chapter's own argument for where NR sits.
- [LOW][widget][static] order_of_operations: The sharpen step's visible blurb in src/chapters/_widgets/OrderOfOperationsWidget.tsx line 35 reads "output, sized to size", a typo shown in the UI; should be "output, sized to the export" or similar.
**Image density:** Correct for a mechanism/workflow chapter. One photo object (the 19.4 BeforeAfter) plus the figure, the widget, and two LightroomPanel replicas is the right diagram-first mix; the single BeforeAfter earns its slot by showing the whole stack run once on one frame, which no schematic could. 1-3 is the target and 1 is fine here. Do not add images.
**Clarity:**
- "A stack, not a rack of sliders", second paragraph: "Work from the whole frame inward... And work from the frame outward first": Two opposite spatial metaphors back-to-back (inward, then outward) describing the chapter's two key rules, then both collapsed into "working outside in". The reader cannot tell which direction maps to which rule, and "work from the frame outward first" is genuinely ambiguous (outward to what?). -> Drop the inward/outward wordplay. State the rules plainly: "Settle the global look before you touch anything local. And fix the shape of the frame, its geometry and crop, before you judge tone inside it. Photographers call the combination working outside in, or global before local."
- "Set the base" section: "exposure places the overall tonal scale and its endpoints, while a contrast curve is an S drawn around a midpoint. Fix the endpoints first...": The anchor switches mid-argument: the earlier stack paragraph says the curve is drawn around a midpoint exposure put in place, then this sentence pivots to "fix the endpoints first". Endpoints vs midpoint is exactly the distinction Ch. tone-and-the-curve teaches, so the slippage reads as an error. In Lightroom, Exposure chiefly moves the midtones; Whites/Blacks set endpoints. -> Keep the midpoint framing consistently: "Fix the midpoint first or you are drawing the curve around a baseline that keeps sliding."
- Widget hint: "a step that now lands on a base a later step already assumed was settled": The sentence garbles the two equivalent views of a broken edge (a step running before its base is settled, vs a base changing under a step that assumed it). It takes three reads to parse, and the widget's own in-UI comment line ("red = a base a later step already assumed") has the same problem. -> Rewrite the hint clause to "a step placed before the base it assumes is settled" and the widget comment line to "// red = a step placed before the base it depends on".
**Verbosity:**
- End of the "go local" paragraph: "Global before local pays off in retuning you never have to do: settle the whole frame once, and each mask gets tuned a single time.": Restates the point made two sentences earlier ("every mask is now aimed at the wrong brightness and has to be retuned"). The retuning cost is stated twice in one short paragraph. -> Cut the closing sentence, or fold it to "Settle the whole frame once and each mask gets tuned once."
- "Detail is the last thing" first paragraph: "Denoise before you sharpen" is stated three times: the wrong-way-round explanation, the standalone "Denoise first, then sharpen.", and the closing invariants sentence. -> Delete the standalone "Denoise first, then sharpen." The closing invariants sentence already lands it.
- "The engine reorders the math" first paragraph, Phocus sentence: The sixteen-bit-TIFF round-trip detail is an aside that stretches an already long paragraph; the round-trip mechanics belong to Sources, not the argument. -> Trim to "Your Leica's DNG and your Hasselblad's 3FR get the same parametric treatment, in Lightroom, Capture One, or Phocus."

## 20. tone-and-the-curve
_Verdict:_ A strong, well-sourced chapter with an excellent photo sequence and widget, but the signature figure's caption and on-figure annotation teach highlight/shadow recovery backwards for the curve actually drawn.
**Top fixes (ranked):**
1. Fix Figure 20.1's recovery teaching: either add/redraw a shoulder pulled below the identity (a second dashed 'recovery' curve, or lower the white-point end) or rewrite the caption and the 'flatten the shoulder -> highlights recover' annotation so the pinned S is described as compressing the ends, with recovery explicitly the shoulder pulled down, matching the widget preset and 20.8.
2. Split the intro's two-decisions sentence into two or three sentences so the black point / white point / contrast structure is parseable on first read.
3. Trim the caption redundancy in 20.2 and 20.3 where they restate the adjacent prose (cut 20.2's final sentence and 20.3's 'protected brightness move' restatement).
4. Gloss 'display-referred' in one clause and replace 'the first... the second' in the Exposure paragraph with named referents.
**Correctness:**
- [HIGH][figure][static] 20.1: The caption ('That is where recovery lives. Flatten the shoulder and near-white inputs map below pure white, so blown highlights come down off the wall. Flatten the toe and the darkest inputs lift off pure black.') and the on-figure annotation 'flatten the shoulder -> highlights recover' are inverted for the curve actually drawn. The figure draws a pinned-endpoint contrast S (tone(x)=x^2.2/(x^2.2+(1-x)^2.2)): tone(0.9)=0.992 and tone(0.95)=0.9985, so near-white inputs map ABOVE the identity, closer to the wall, and tone(0.1)=0.008 pushes darks toward black. Recovery requires a shoulder pulled BELOW the diagonal (as the widget's 'recover the ends' preset and LightroomCurve 20.8 correctly show). The chapter's own budget section even says the S makes the ends 'lose separation'. The signature figure teaches the chapter's core move backwards.
- [LOW][caption][static] 20.6: Missing the meta prop; every other image object in the chapter (20.2 through 20.5) carries one, so the meta line will be inconsistently absent under this one figure.
- [LOW][widget][static] tone_curve: Hint says 'flatten the shoulder to pull the bright sky back down off the right wall', but at the initial linear curve nothing is blown (sky band peaks at 0.99, blown threshold is 0.996), so a reader who flattens the shoulder first watches the readout stay at 0%. The hint's move only pays off after lifting the curve. Reword to make the sequence explicit ('lift the whole shape until the sky blows, then flatten the shoulder to bring it back').
**Image density:** Right count and mix for a visually-led develop chapter: 5 distinct image objects (BeforeAfter 20.2, 20.3, 20.4, 20.6 plus AnnotatedPhoto 20.5) with LightroomPanel 20.7 and LightroomCurve 20.8 tying the same edit together. No redundancy: reusing hazy-cityscape for 20.2 vs 20.4 is deliberate and strong (same frame, two different handles), and 20.5 vs 20.6 share a dark-interior-bright-window subject but do different jobs (reading the walls vs recovering them).
  - idea: A small Photo or AnnotatedPhoto of a partly clipped highlight gone magenta (e.g. a blown sunset or lamp with the magenta fringe labelled) (where: Recovery section, beside the paragraph explaining channel-by-channel saturation and the magenta drift; why: The magenta-highlight mechanism is the chapter's most visual unillustrated claim; one labelled example would anchor it better than the prose alone.)
  - idea: Optional: a real before/after pair (two exported files, not a live filter) of an extreme shadow lift surfacing noise (where: End of the recovery section or beside Exercise 04; why: The noise-floor limit is asserted but never shown; a CSS/svg filter cannot honestly simulate noise, so only do this with a genuine processed pair.)
**Clarity:**
- Intro, 'the two decisions that matter most in it: where the ends of the range fall, which is the black point, the white point, and how much detail you can pull back near them, and how steeply...': Comma pileup; the two decisions and their three sub-clauses are hard to separate on first read. -> Split into two sentences: 'This chapter is about that shape and the two decisions inside it. The first is where the ends of the range fall: the black point, the white point, and how much detail you can pull back near them. The second is how steeply the tones between them are spread, which is contrast.'
- Exposure section, 'The slider is closer to the first than the second, but the ends are still yours to lose.': 'The first' and 'the second' point back across three sentences to two behaviors that were never labelled, so the referents are ambiguous. -> Name them: 'The slider is closer to the pinned move than to the everything-shifts move, but the ends are still yours to lose.'
- Recovery section, 'because the on-screen histogram is display-referred': 'Display-referred' is unexplained jargon at the exact moment the reader needs the distinction. -> Add one gloss clause: 'display-referred, computed after the file has been mapped for the screen, so it reflects the preview rather than the raw.'
- Caption 20.4, 'this leaves the middle straight and only moves the two ends': The visible snap comes as much from the middle steepening (slope 1.9 everywhere) as from the endpoints; 'only moves the two ends' can read as 'the midtones are untouched.' -> Add half a clause: '...only moves the two ends, which steepens the whole straight line at once, so the midtones gain contrast too.'
**Verbosity:**
- Caption 20.2, final two sentences: 'It buys that midtone separation from the ends, which is the contrast budget in a sentence' and the next sentence say the same thing twice, and both restate the prose paragraph directly above. -> Cut the final sentence ('Steepen the middle and the darkest and lightest tones give up some separation to pay for it.').
- Caption 20.3, middle sentence: 'That is the protected brightness move: with the endpoints pinned you place the bulk of the histogram where you want it before shaping anything around it' duplicates the prose two paragraphs up nearly verbatim. -> Compress to 'That is the protected brightness move.' and let the prose carry the rest.
- Figure 20.1 caption: Seven sentences that retell most of the chapter; the figure caption is doing the intro's, the budget section's, and the recovery section's jobs at once. -> Trim to axes, identity, slope-is-contrast, and the two endpoints; drop the recovery sentences (which are also wrong for the drawn curve, see correctness).

## 21. color-grading
_Verdict:_ A well-sourced, cleanly structured chapter with an honest widget and strong correct-then-choose framing, but it sits one image object below the visually-led target and two of its three live BeforeAfter demos under-deliver on what their captions claim.
**Top fixes (ranked):**
1. Strengthen the 21.2 neutralizing matrix (roughly R ~0.75 / B ~1.5, tuned by eye) so the after side actually reads neutral; the current 0.9/1.12 leaves the tungsten portrait visibly warm under a label that says 'neutralized' and a caption that says 'fully'.
2. Extend the 21.5 afterClip polygon to cover the whole visible hull, including the bow tip, the lower hull edge, and the stern section behind the dock posts, so the boat darkens as one object as the caption claims.
3. Add a fifth image object teaching a point no current image covers: a green-cast/tint-axis correction wipe or a Vibrance-vs-Saturation skin-protection wipe (both are claimed in prose and shown nowhere), bringing the chapter to the visually-led 5+ target.
4. In ColorGradingFigure, angle the shadows handle toward the teal side and the highlights handle toward the orange side of their wheels so angle carries hue as the chapter teaches; while there, trim the 21.1 caption to three or four sentences.
**Correctness:**
- [HIGH][caption][browser] 21.2: The afterLabel "neutralized" and caption claim "Neutralize it fully, as here" are not supported by the filter: feColorMatrix gains of R 0.9 / B 1.12 give roughly a 0.3 EV blue-red swing, while the chapter's own FastRawViewer citation two paragraphs earlier says correcting a ~2500 K cast takes about a stop and a half of blue lift. The after side of this strongly orange tungsten portrait will still read clearly warm, contradicting both the label and the 'you have a document' claim.
- [MED][honesty][browser] 21.5: afterClip is present (good, the local claim is honest in kind) but the polygon (x 47-68%, y 42-56%) under-covers the boat: the bow tip (~44-47%), the lower hull (down to ~60%), and the entire stern section visible between the dock posts (x 68% to ~95%) fall outside it. On the after side a large piece of the boat stays bright blue while the caption says 'the boat sinks toward a deep navy', which visibly breaks the demo.
- [MED][figure][static] 21.1: In the signature figure's grading-wheels card, the shadows (teal) and highlights (orange) handles are both pushed at the same up-right angle, with only dot color carrying the hue (the code comment admits 'push up-right; hue carries the meaning'). The chapter teaches that the handle's angle IS the hue and that the whole grade works because teal and orange sit across a diameter; same-direction handles quietly contradict both.
- [LOW][caption][static] 21.8: "places subject and background on opposite diameters" is geometrically muddled; they sit at opposite ends of one diameter (21.6 says 'across a diameter' correctly).
**Image density:** At 4 photo-based image objects (1 Photo + 3 BeforeAfter; the LightroomPanel/HSL replicas do not count toward the target), the chapter is one short of the 5+ target for a visually-led chapter, and it uses no AnnotatedPhoto anywhere. The four it has each teach a distinct stage of the stack, though 21.6 and 21.7 both live on teal-and-orange; 21.7 earns its place only because its caption pivots to grade-vs-correction recognition.
  - idea: A BeforeAfter demonstrating the tint axis: a fluorescent or mixed-light frame with a green cast, corrected with magenta tint. (where: White balance is the one correction, after the temperature demo (21.2); why: The prose gives tint real airtime (fluorescent and mixed light 'demand' it, no temperature setting can undo it) but every image in the chapter demonstrates only the blue-amber axis.)
  - idea: A Vibrance vs flat Saturation wipe on a portrait with warm skin, afterFilter or paired renders showing skin blowing out under Saturation but holding under Vibrance. (where: Then the choices: the mixer and the grading wheels, after the mixer paragraph; why: The Vibrance-protects-skin claim is the paragraph's punchline and the Callout repeats it, yet no image shows it; it is exactly the kind of claim a wipe proves in one drag.)
  - idea: If adding both is too many, consider making one of them an AnnotatedPhoto (e.g. labels on the warm-lit face vs the cool surround of 21.7 marking which wheel zone each region falls into). (where: 21.7 or its replacement; why: The chapter has no AnnotatedPhoto, and pointing at which pixels the shadows wheel vs highlights wheel actually grabs would teach zone targeting more concretely than prose.)
**Clarity:**
- "The move is nearly free, and only on a raw file." (opening of the per-channel-gain paragraph): Reads as a broken fragment; the restriction is doing load-bearing work but the syntax hides it. -> Rewrite as "The move is nearly free, but only on a raw file." or "On a raw file the move is nearly free."
- "This stage is exactly where the two cameras this book is built around start from different color, by design." (profile section): Tangled word order; "start from different color" is awkward and the sentence takes two reads. -> "This stage is where the two cameras in this book part ways on color, by design."
- BeforeAfter 21.4 labels "color profile" / "standard profile": "color profile" is generic to the point of confusion, since every profile is a color profile; the labels only make sense after reading the caption's Adobe Color vs Adobe Standard framing. -> Label them by character, e.g. beforeLabel="warm, saturated profile" afterLabel="flat, standard profile", or name the renderings the caption already implies.
**Verbosity:**
- Captions 21.6, 21.7, 21.8 plus the grading-wheels paragraph: The complements-across-the-wheel point ("subject and surround across a diameter, not because teal and orange are magic") is stated four times in near-identical words. -> Keep it in the prose and in 21.6; cut the diameter sentence from 21.7 and end 21.8 after the aqua/blue description.
- Figure 21.1 caption: Nine sentences that restate the entire chapter thesis the surrounding prose (and the figure's own tags) already carry. -> Cut to three or four sentences: the four stages in order, the one correction flagged in accent, correct first then choose.
- "About 5000 K is roughly neutral.": Double hedge ("About" and "roughly"). -> "Around 5000 K is neutral."

## 22. local-adjustments-and-masking
_Verdict:_ A strong, honest chapter that hits the visually-led target with a distinct job per image; the risks are one afterClip polygon that likely darkens the subject's raised hand and a changelog-dense AI-masking paragraph plus triple restatement of the four-sources list.
**Top fixes (ranked):**
1. Fix the 22.7 afterClip polygon so the boundary genuinely excludes the raised hand (raise the notch to roughly x 43-58% at the fingers' top, ~y 31%), then re-verify in the browser; the caption's claim "nothing on the man himself changes" is currently at odds with the geometry.
2. Extend the 22.6 afterClip rightward (to about x 63%) so the dodge covers the whole face the caption says it covers, verifying the edge against the live wipe.
3. Compress the AI-masking history paragraph: lead with the idea, collapse the two dated releases into one sentence, and cut the ten-item People-mask anatomy list to a five-word clause; the exact versions live in Sources already.
4. De-duplicate the four-sources material: trim Figure 22.1's caption to the equation and the white/grey/black reading, and shorten the Callout's four-sources sentence, leaving the "four ways" section as the single full treatment.
**Correctness:**
- [HIGH][honesty][browser] 22.7: The afterClip polygon likely darkens the top of the man's raised hand while the caption stakes its point on "nothing on the man himself changes" and "a mask that stops at his outline". Viewing the photo, his outstretched fingers sit around x 44-58%, y 32-40%, but the clip boundary in that x range runs at y 40-44% (points 45%/40%, 50%/40%, 60%/44%), putting the upper hand inside the darkened, desaturated after region. The notch at (42,31) appears aimed at the hand but is placed too far left, over the hair.
- [MED][overlay][browser] 22.6: The afterClip's right edge tops out at x 55%, but the woman's face extends to roughly x 63-66% in the photo, so the dodge likely stops mid-cheek and leaves the viewer-right side of her face unlifted. The caption concedes a visible hard edge but claims "the where is her face"; a mask missing a third of the face reads as sloppy rather than deliberately hard-edged.
- [LOW][caption][static] 22.3: Missing the meta attribute; every other photo object in the chapter (22.2, 22.5, 22.6, 22.7) carries one.
**Image density:** Right count and a good mix for a visually-led develop chapter: 5 image objects (AnnotatedPhoto 22.2, 22.5; BeforeAfter 22.3, 22.6, 22.7) plus the LightroomPanel, each with a distinct job (the where drawn on a scene, the burn live, a natural dodge, a masked dodge with a visible edge, sinking the surround). Mild conceptual overlap: 22.2 and 22.5 both teach "nature already performed the dodge and burn" (landscape burn, portrait dodge); tolerable because they anchor different sections, but they are the pair to merge if the chapter ever needs trimming. The real gap is that the third mask kind gets no visual.
  - idea: A visual for the range mask: a BeforeAfter (or two-state AnnotatedPhoto) on a frame where a ridge or tree breaks into the sky, contrasting a gradient dimming the intruding shape vs a luminance range skipping it. This is exactly Exercise 03's punchline and the one mask type argued in prose only. (where: End of "The where: four ways to draw the same selection", after 22.4; why: The brush/gradient and AI kinds each get photographic support; the range mask, the chapter's own example of an edge "no gradient could follow", has none.)
  - idea: Show an actual grayscale mask once: a small inset or AnnotatedPhoto state rendering the sky selection of 22.2 as the white/grey/black alpha image itself, not a red overlay. (where: Section "A local edit is a mask times an adjustment" or as a second overlay state on 22.2; why: The chapter's foundational claim is "a mask is a grayscale selection, white reveals, black conceals", yet every mask shown is a red tint; the reader never sees the thing being defined.)
**Clarity:**
- 22.3 caption: "which is what a linear gradient keyed to the sky does when you drag it down": Conflates two different mask types the chapter just carefully separated. A linear gradient selects by place; it is not "keyed" to anything. The tonal weighting the caption describes (brights pulled hardest, darks held) comes from the Highlights/Whites values in panel 22.4, not from the gradient itself. Coming one paragraph after 22.2 correctly offers "a luminance range or a linear gradient" as alternatives, this blurs the place-vs-content distinction the section exists to teach. -> Attribute the tonal behavior to the sliders riding the mask: "...which is what the Highlights and Whites moves in the panel below do when they ride a gradient dragged down from the top."
- Section "The where", the AI-selections passage ("Adobe's October 2021 masking overhaul... Lightroom Classic 11 and Camera Raw 14... Adobe MAX in October 2022..."): The teaching point (AI collapsed the cost of a hard outline) is buried under release-note detail: two dated releases, two version numbers, five feature names, then a ten-item anatomy inventory. It reads like a changelog, and the reader has to dig for the idea the next paragraph builds on. -> Lead with the point, compress the history to one sentence ("Adobe folded everything into one Masking panel in 2021 and has been adding one-click selections since: subject, sky, people, objects, background"), and keep the exact versions and dates in Sources, where they already are.
**Verbosity:**
- Figure 22.1 caption ("A local edit factored into its two independent parts..."): At roughly 190 words it re-narrates every panel of the figure and then previews the entire next section (the four sources, add/subtract/intersect, "choose the mask that draws the edge most cheaply"). The four-sources material now appears three times: figure caption, the "four ways" section, and the Callout. -> Cut the caption to the equation and the white/grey/black reading (first three sentences). Drop the "Below, the where has four sources..." sentences; the figure's own cards and the following section carry that load.
- People-mask parts list: "facial skin, body skin, eyebrows, the whites of the eyes, the iris and pupil, lips, teeth, and hair, with clothing and facial hair added in 2023": A ten-item anatomical inventory where the point is only that the People mask subdivides a person finely. -> Compress to "breaking a person into separately selectable parts, down to skin, eyes, lips, teeth, and hair"; the full list is already in Sources.
- Callout, middle sentence ("The where has four sources... and they combine with add, subtract, and intersect"): Third full recital of the four sources plus set operations in one chapter. -> Trim to "The where has four sources (brush, gradient, range, AI select) that combine with add, subtract, and intersect" or drop the sentence; keep the saliency and Adams sentences, which are the callout's real payload.
- 22.2 caption, last sentence ("The scene is a finished dodge and burn made of weather..."): Restates the caption's own opening line ("Light did here what a mask does in software") in different words. -> Delete the final sentence; end on the dodge/burn recipe sentence.

## 23. black-and-white-conversion
_Verdict:_ A rich, well-sourced chapter with an honest figure and a genuinely useful widget, undermined by a centerpiece triptych whose red-filter panel does not show the dramatic swap its caption sells, plus a split-tone lesson that repeats the same formula five times.
**Top fixes (ranked):**
1. Fix the 23.3 triptych: re-render the middle panel with a much stronger red-filter mix on a scene whose sky is actually deep blue (the current pale sky and dark barn mathematically cannot swap), or rewrite the caption and alt to what the panel truly shows; then reconcile 23.4's 'sink the sky toward black' language with it.
2. Correct caption 23.6: the sky in the toned panel is warm near the roofline and neutral at the top, not cool; drop 'and the sky' and soften 'kept quiet' or lower the shadow saturation in the render.
3. De-duplicate the split-tone formula: 'quiet enough to read as toned, not colored' appears five times and 'teal and orange' three times; keep each once in prose and at most once in a caption.
4. Illustrate the banding-to-dither claim with a BeforeAfter (posterized smooth sky vs the same sky with grain), funding the slot by cutting or repurposing the redundant 23.6 Burano pair.
**Correctness:**
- [HIGH][caption][static] 23.3: Caption claims the red-filter panel 'drives the sky almost to black and lifts the barn wall bright' and that the colored things 'swap' around the silo; the alt repeats 'trade places from light to dark'. Measured pixels in the red-mix panel: sky 106-127, barn wall ~105. The two CONVERGE to nearly the same mid-grey; the sky is nowhere near black, the barn is not bright, and no visible swap happens. The scene itself cannot deliver the figure's crossing: the source sky is a pale blue (94,145,199) and the barn a dark red (121,57,62), so even a true red filter lands both near 105-110. The blue-mix panel does match its 'pale sky, muddy dark barn' claim. This photo is captioned 'the figure made real' and it is precisely the figure's story it fails to show.
- [MED][caption][static] 23.6: Caption says the print is 'cool in the shaded foreground and the sky'. Measured: the sky is near-neutral at the top (sat 0.01) and clearly WARM near the roofline (hue ~39, sat 0.19); only the foreground and shadows are cool. The caption sends the reader to look for coolness where the photo shows warmth. Secondary: 'the tone is kept quiet' is strained by deep shadows at sat ~0.84 (rgb 6,18,71 in the doorway), which read as a strong navy.
- [MED][caption][static] 23.4: Caption presents these sliders as 'the mix behind the red-filter panel above' with blue at -70 to 'sink the sky toward black', but the triptych's red panel shows a mid-grey sky (measured 106-127). The two exhibits contradict each other; whichever is fixed (re-render 23.3 or soften 23.4), they must agree.
**Image density:** Count and mix are right for a visually-led chapter: 5 image objects (three BeforeAfter, one AnnotatedPhoto, one Photo) plus the figure, widget, two Lightroom panels, and a curve. But 23.6 and 23.7 both demonstrate the identical cool-shadow/warm-highlight split, and the chapter's most visual unillustrated claim, grain-as-dither breaking a banded sky, has no image at all.
  - idea: A BeforeAfter of a smooth blue-hour sky posterized into visible bands on one side and the same sky with grain dithering the steps away on the other (afterFilter or a second render). (where: Grain section, replacing or following the current text-only banding claim; why: The chapter twice promises that grain breaks banding into a smooth gradient; this is the single most visual claim in the chapter and it is the only one taught with no picture.)
  - idea: Cut or repurpose 23.6 (the Burano pair): 23.7 already teaches the same split-tone recipe as a wipe, and 23.8 shows the exact slider values behind it. (where: Split-tone section; why: Two photos plus a panel plus the widget all teaching the identical cool/warm split is redundant; freeing 23.6's slot funds the banding demo without inflating the chapter.)
**Clarity:**
- Grain section, 'Past a Size of about 25 the tool quietly mixes in a little blue so the grain sits better next to noise reduction.': The clause 'sits better next to noise reduction' is opaque. The reader cannot work out why blue helps or what 'next to noise reduction' means; it reads like a half-remembered release note. -> Either explain in one clause (blue-channel speckle survives noise reduction more believably, so large grain reads as texture rather than artifact) or cut the sentence; it is a footnote-level fact.
- Section 'Two cameras, and the sensor that throws color away', final paragraph ('Whatever you convert in, the tools converge...'): The Silver Efex / FilmPack / Capture One survey sits under a heading about cameras and sensors; it belongs with the editor-comparison material in the mix section, and the jump from Monochrom sensor philosophy back to plugin catalogs is jarring. -> Move the tools paragraph up to the end of the mix section next to the Photoshop / Capture One comparison, or give it its own one-line bridge so the sensor section ends on the film-bargain point, which is its strongest sentence.
**Verbosity:**
- 'kept quiet enough to read as a toned print and not a color one' (prose line ~100, captions 23.6, 23.7, 23.8, and Exercise 04): The same quiet-toning formula appears five times in one chapter, nearly verbatim. By the third occurrence it reads as boilerplate. -> Keep it once in the prose where split toning is defined and once at most in a caption; rewrite or cut the other three.
- 'the monochrome cousin of teal and orange' (prose ~100), 'the mono version of teal and orange' (caption 23.6), 'the monochrome cousin of teal and orange' (caption 23.7): The identical teal-and-orange analogy is made three times. -> Use it once in the prose; the captions can point at the photo instead of re-making the analogy.
- Grain-as-dither: banding warning (~line 60), 'A little grain is dither...' (~line 64), and the last sentence of caption 23.5: The dither point is stated three times; caption 23.5's closing sentence adds nothing the paragraph above it did not already say. -> Cut the final sentence of caption 23.5 ('The same speckle sprinkled over a banded sky...').
- 'Two cameras' opening paragraph: 'A raw capture keeps every channel the sensor read, so the conversion is a set of instructions applied at development, not a change written into the pixels...': Nearly restates the intro paragraph and the mix section's raw-file point; the raw-keeps-color promise is made four times across the chapter including the callout. -> Compress this paragraph to one sentence and let the section get to the two bodies faster.

## 24. output-and-print
_Verdict:_ A tight, accurate, well-sourced workflow chapter with a strong figure and a well-chosen single photo; the only substantive defect is the widget's black point compensation math, which contradicts the chapter's own account of relative colorimetric, plus a halo-arithmetic slip and some late-chapter repetition.
**Top fixes (ranked):**
1. Fix the widget's BPC tone math in src/chapters/_widgets/OutputAndPrintWidget.tsx: with BPC on, relative colorimetric currently rescales all tones exactly like perceptual and never clips highlights; change to something like yOut = Math.min(paper.black + y * (1 - paper.black), paper.white) so shadows rescale, in-gamut midtones stay put, and highlights above paper white still clip, matching the prose and the widget's own footer claim.
2. Correct the sharpening-halo arithmetic: "between a fiftieth and a hundredth" of an inch at 300 ppi is roughly three to six pixels, not "roughly three pixels".
3. Deduplicate the "print arriving early" beat: end caption 24.2 after "the whole frame flattens" plus one short closing clause, and let the post-widget paragraph and panel 24.3 carry the proof-copy correction advice (currently stated four times).
4. Cut the duplicated Q3/X2D print-size figures from Exercise 02 so the exercise asks the reader to compute rather than handing back numbers from two sections earlier.
**Correctness:**
- [MED][widget][static] soft_proof_bench: The black point compensation branch in OutputAndPrintWidget.tsx (proof(), line ~148: yOut = Math.min(paper.black + y * span, paper.white)) rescales the ENTIRE tonal range, identical to the perceptual branch, and the Math.min never binds (paper.black + y*span <= paper.white for all y <= 1). Consequences: the code comment "highlights still clip" is false; with BPC on (the default), every in-gamut midtone moves under relative colorimetric, directly contradicting the chapter prose ("leaves every color that already fits exactly where it was"), the widget hint ("relative colorimetric keeps the fitting colors exact"), and the widget's own footer text. A reader toggling perceptual vs relative with BPC on sees only chroma change, which misteaches the intent distinction. Fix: rescale only the black end and keep the highlight clip, e.g. yOut = Math.min(paper.black + y * (1 - paper.black), paper.white).
- [LOW][aitell][static] 24.2: Caption 24.2 ("That duller preview is not a worse edit. It is the print arriving early") and the prose above it ("That flatness is not a bug. It is the print, arriving early") both use the "not A. It is B" construction, kin of the banned "it's not just A, it's B" tell; used once it reads as house voice, doubled it reads as a template.
**Image density:** Correct for a mechanism/workflow chapter. One photo object (the Kabukicho BeforeAfter) plus the one Figure, one Widget, and one LightroomPanel is exactly right at the 1-3 target, and the single photo earns its slot: a worst-case saturated-neon subject is the one place a real photograph beats the schematic for showing what soft proofing does. The Figure and Widget divide the work cleanly (static gamut geometry vs. interactive remap). Do not add images.
**Clarity:**
- Output sharpening section: "a sharpening halo a fixed fraction of an inch wide, between a fiftieth and a hundredth, which at 300 ppi works out to roughly three pixels": The arithmetic only matches one end of the stated range. 1/100 inch at 300 ppi is 3 pixels, but 1/50 inch is 6 pixels, so "between a fiftieth and a hundredth" works out to roughly three to six pixels, not three. -> Either say "which at 300 ppi works out to roughly three to six pixels" or narrow the range to "around a hundredth of an inch, roughly three pixels at 300 ppi".
- Opening paragraph: "Three things change in the copy, its size, its color space, and the sharpening that suits the medium, and a fourth change waits...": The comma-spliced appositive list is hard to parse on first read; "the copy, its size" momentarily reads as a two-item list before the sentence resolves, and the trailing "and a fourth change" clause piles on. -> Use a colon and a sentence break: "Three things change in the copy: its size, its color space, and the sharpening that suits the medium. A fourth change waits for anything you print..."
- Dmax paragraph: "defined as the negative base-ten logarithm of how little light the black patch reflects": "how little light" reads backwards next to a negative logarithm; the reader has to untangle whether more density means more or less reflected light. -> Say "the negative base-ten logarithm of the fraction of light the black patch reflects".
**Verbosity:**
- Caption 24.2 last sentence vs. soft-proofing prose: "It is the print arriving early, while you can still lift the shadows and add back a little saturation" / "That flatness is not a bug. It is the print, arriving early enough to do something about.": The same "not a worse thing, it is the print arriving early" beat lands twice within one screen, and the caption also pre-states the proof-copy corrections that reappear in the post-widget paragraph AND in caption 24.3. The corrective move (lift shadows, add contrast/saturation) is stated four times counting Exercise 03. -> End caption 24.2 at "...so the whole frame flattens. That duller preview is the print arriving early." and let the post-widget paragraph and panel 24.3 carry the correction advice.
- Exercise 02: "For the Q3 that is about 31.7 by 21.1 inches at 300 ppi and 39.7 by 26.4 at 240; for the X2D II it is about 38.9 by 29.1 and 48.6 by 36.4.": Repeats verbatim the four print-size figures given two sections earlier, and hands the reader the answer to the division the exercise just asked them to do. -> Cut the numbers: "Take your camera's pixel dimensions and divide each by 300, then by 240, and check your result against the numbers earlier in the chapter."
- Callout: At roughly 190 words the recap restates every section in full, including the four moves already named twice in the body and the "300 up close and less from afar" detail. -> Trim by about a third: cut the four-move re-listing to a single clause and drop the ppi specifics, keeping the master/copies frame, the three-ways-smaller box, the two intents, and the edit-wide-deliver-right rule.

## 25. portrait-in-practice
_Verdict:_ A strong, well-mapped synthesis chapter (5 distinct image objects, clean prose, no gate violations) whose weak spots are overlay precision on the short-light photo, zero photographs for its self-declared most important link (distance/perspective), and a figure whose shoot order contradicts the section order.
**Top fixes (ranked):**
1. Fix the 25.5 overlay in the MDX: move the terminator trace to the real lit-to-shadow boundary (roughly x 58-63 running down from the temple past the cheek) and pull the 'catchlight' chip from (27,39) to adjacent to the marked eye; verify placements live in the browser.
2. Add a perspective-distortion image pair (close 28mm vs stepped-back short tele, same crop) to 'Distance first, then the lens'; it is the only chain link with no photograph and the chapter names it the most common portrait mistake.
3. Reorder the SHOOT row in PortraitInPracticeFigure.tsx (and caption 25.1) to distance -> near eye -> light -> fill so the signature figure matches the chapter's section order.
4. Tighten caption duplication: make 25.2 develop-only, cut 25.6's final leaf-shutter clause and 25.7's melanin/blood repeat, and either replace or re-grade the heavily teal 25.7 source so 'a faint cast, a small fix' is actually what the wipe shows.
**Correctness:**
- [HIGH][overlay][browser] 25.5: The accent path meant to trace the terminator runs at x=44-47 (down the lit side of the nose to the chin), but the actual lit-to-shadow boundary in the photo, and the 'terminator' chip itself, sit around x=58-63 on the far (viewer-right) side of the face. The drawn line and its own label disagree by ~13 units, and the line as drawn reads as a nose contour in fully lit skin, not the terminator the caption describes.
- [MED][overlay][browser] 25.5: The 'catchlight' chip sits at (27,39), floating in the black background well off the face, while the circle marking the catchlight is at (40,43) on the viewer-left eye. Chips render centered at (x,y) with no leader line (AnnotatedPhoto.tsx), so this label points at nothing.
- [MED][caption][browser] 25.7: Caption calls the as-shot side 'a faint cool, slightly green cast... without looking obviously wrong', but the source photo carries a heavy, obviously stylized teal-green grade (visible in the white building and across the skin). The small correction matrix (R x1.07, G x0.97, B x0.95) likely cannot land skin from that starting point, so the 'skin landed' side may still read graded and undercut the 'the fix is small' lesson.
- [LOW][overlay][browser] 25.6: The 'shadow side' chip at (21,52) sits on the mass of red hair covering the viewer-left of her face, not on a visible cheek, and 'lit cheek' at (46,56) lands at the jaw/chin line rather than the cheek. The face is also lit quite evenly (direct-flash look), so both chips need a nudge onto actual skin that shows the difference they name.
- [LOW][overlay][browser] 25.4: The 'background falls off' chip at (76,18) appears to sit at the boundary of her dark hair rather than clearly on the blurred background; the unambiguous bokeh is further toward the top-right corner.
**Image density:** Count and mix are right for a visually-led chapter: 5 image objects (3 AnnotatedPhoto, 2 BeforeAfter) plus a LightroomPanel, each mapped to one distinct chain link (develop, focus plane, light pattern, fill vs sky, skin tone). The one real gap: the chapter's first and loudest link, distance/perspective ('the most common portrait mistake'), has no photograph at all, while the two window-light portraits (25.2, 25.4) overlap in caption content (both teach near eye + catchlight).
  - idea: Add a two-frame perspective comparison (BeforeAfter or paired Photos): same head-and-shoulders crop shot close with a wide lens vs stepped back with a short tele, showing the nose/ear proportion change. (where: 'Distance first, then the lens', after the ballooning paragraph; why: It is the chapter's self-declared most common mistake and the only chain link taught with zero images; Exercise 01 even assigns the reader to shoot exactly this pair. An ideal slot for the author's own Q3-at-2ft vs X2D-90V pair.)
  - idea: Consider one photo of loop or Rembrandt lighting to complement 25.5, which sits near the split end of the range. (where: 'Put the light where it shapes the face'; why: The prose recommends loop as 'the most versatile and natural' default, but the only example shown is the dramatic exception; the reader never sees the pattern they are told to start with. Optional, only if a strong licensed frame exists.)
  - idea: Differentiate rather than cut: rewrite 25.2's caption to be develop-only. (where: BeforeAfter 25.2; why: Removes the 25.2/25.4 teaching overlap without losing an image; both photos earn their slots once their captions stop teaching the same two points.)
**Clarity:**
- Figure 25.1 vs section order: The figure runs the shoot as lens/distance -> light & ratio -> leaf-shutter fill -> focus the near eye, but the chapter's sections teach distance -> near eye -> light -> fill. The intro enumeration ('from the distance... through the light and the leaf-shutter fill... to the skin tone') also skips focus entirely. A reader cross-referencing the figure against the sections gets two different chains. -> Reorder the SHOOT cards in PortraitInPracticeFigure.tsx (and the 25.1 caption) to distance -> near eye -> light -> fill so the figure, the intro sentence, and the section order agree.
- 'The XCD 90V frames like a 71mm': The medium-format-to-full-frame equivalence is unstated; a reader who forgot the 0.79 crop factor will read '90V frames like a 71mm' as a contradiction. -> Add three words: 'frames like a 71mm in full-frame terms'.
- 'from about 1.70 at 50mm to about 1.80 at 85 to 105mm, a very large change': 1.70 to 1.80 looks tiny on the page; asserting 'a very large change' without context invites skepticism instead of conviction. -> Replace the editorial with the consequence: 'a shift large enough that viewers rated the 105mm faces most attractive and the 50mm faces least.' (Then drop the duplicate rating clause that follows.)
- 'a flash exposure compensation near minus one and two-thirds being a common choice': Spelled-out fraction in an absolute-participle clause is clunky and slows the one practical number in the paragraph. -> Rewrite: 'a common starting point is flash compensation around minus 1 2/3 stops'.
**Verbosity:**
- Figure 25.1 caption: The caption narrates every card in the figure (~110 words), duplicating what the figure itself and the aria-label already say. -> Cut to the claim: the chain, the split at the shutter, and 'every link points at the face'; drop the card-by-card list and the gear sentence.
- BeforeAfter 25.2 caption, last two sentences: The caption re-teaches near-eye focus and the single catchlight, which is exactly what AnnotatedPhoto 25.4 then teaches two figures later. The develop is this image's job. -> End the caption after the S-curve sentence; let 25.4 own focus and catchlight.
- AnnotatedPhoto 25.6 caption, final clause: 'and a fast-syncing leaf shutter is what lets you force that balance...' repeats the leaf-shutter argument the preceding paragraph just made at length. -> End the caption at 'keep the face up and let the background fall.'
- BeforeAfter 25.7 caption: 'its brightness and saturation set by melanin but its hue set by the blood beneath' restates the skin-tone-line paragraph two paragraphs earlier almost verbatim. -> Cut that clause; keep 'the skin settles onto the narrow red-to-yellow line every complexion shares.'
- Callout: At ~150 words the callout re-summarizes every section including numbers already given twice; it reads as a second conclusion. -> Trim by a third: one sentence per link, drop the leaf-shutter comparison and the 28mm/XCD restatement.

## 26. street-and-documentary
_Verdict:_ A tightly argued, factually solid chapter whose mechanics and widget all check out; the real work left is one misplaced overlay, one over-claimed caption, a triple-stated anticipation point, and a documentary half that carries zero photographs.
**Top fixes (ranked):**
1. Fix the 26.6 layer overlay: move "middle: the street" (and its underline) off the building canopy onto the mid-ground walkers/cars (~x60, y62), and reconcile "far: the city" with the caption's far-end-of-the-avenue haze.
2. Rewrite the 26.4 caption to teach environment-tells-the-story without asserting a wide lens used close (unsupported by the photo), and simultaneously cut the duplicate wide-close/step-back framing from 26.6's caption so 26.4 owns embedding and 26.6 owns layering.
3. Add one public-domain documentary photograph (e.g. Lange/FSA via Wikimedia Commons) as a plain <Photo> in the street-vs-documentary section, the chapter's only imageless section and its second titled subject.
4. De-duplicate the anticipation point: cut it from the closing Callout and halve the widget hint, which restates the preceding paragraph nearly sentence for sentence.
**Correctness:**
- [MED][overlay][browser] 26.6: The "middle: the street" label at (72,40) with its underline at y47 sits on the dark canopy/arcade of the right-hand building, not on the mid-ground street; the moving street with its walkers and the parked cars the caption names sit lower and more central, roughly x55-70, y55-72.
- [MED][caption][static] 26.4: Caption asserts "A wide lens used close" and "The 28mm keeps you in it," but the photo is a flat, frontal frame with no wide-angle cues (no looming foreground, no edge stretch); it reads as a normal focal length shot from across the lane. The embedding lesson holds; the specific lens-and-distance claim is unsupported by the image, and 26.6 already carries the genuine wide-close proof.
- [LOW][overlay][browser] 26.6: "far: the city" at (33,20) marks the buildings directly across the street at upper left, while the caption defines the far layer as "the buildings fading into haze at the far end" of the avenue, which vanishes near center (~x62, y57). Label and caption point at different features.
- [LOW][overlay][browser] 26.3: "far end still sharp" at (48,33) appears to sit in the blown-out bright haze at the street's end where sharpness cannot be read; it wants to sit on the distant shopfronts where detail is actually visible.
- [LOW][widget][browser] catch_the_moment: The walker (stroke #090c0d) is near-invisible against the hardcoded dark wall (#182124) and pillars (#0c1114) outside the lit doorway, and the default pct=28 places the walker behind the left pillar, so the widget's initial state can read as an empty scene. Possibly intentional silhouette staging, but worth a look.
**Image density:** Count is right for a visually-led chapter: 5 image objects plus the figure and widget, and each nominally teaches a distinct point (geometry-lock, deep zone, embedding, fishing, layering). Two problems with the mix: all five are AnnotatedPhoto (no plain Photo, no BeforeAfter), and 26.4/26.6 partially overlap on the wide-lens-used-close lesson. The bigger gap: the entire documentary half of the chapter (Street, documentary, and the right to the picture) has zero images even though it names Frank, Lange, Maier, Moriyama, and Gilden.
  - idea: Add one plain <Photo> of the documentary tradition, e.g. a public-domain Dorothea Lange FSA photograph from Wikimedia Commons, in the street-vs-documentary section. (where: Street, documentary, and the right to the picture; why: It is the only imageless section, it covers the chapter's second titled subject, the FSA passage hands you a licensable public-domain image, and a plain Photo varies the all-AnnotatedPhoto mix.)
  - idea: Demote 26.5 (staircase-geometry) from AnnotatedPhoto to plain Photo. (where: Fishing, not hunting; why: Its overlay is just two rects and one label pointing at figures the eye finds instantly; the graphic photo teaches on its own, and dropping the overlay reduces annotation fatigue across five overlaid photos in a row.)
**Clarity:**
- "It reframes the story. It does not compress it." (end of the crop-framelines paragraph in The camera as a reflex): This re-attaches compression to focal length, which the book explicitly debunked one section earlier ("the swelling of what is near... come from being close, not from the focal length"). A native 90mm from the same spot would render the identical perspective as the crop, so "does not compress" is misleading as a crop-vs-lens contrast. -> Tie it to distance, not glass: "It reframes the story from where you stand; a telephoto's compression only ever came from standing farther back." Or cut the compression clause and end on the depth-of-field point, which is the true difference.
**Verbosity:**
- "By the time you see it, you are already late" (widget intro), "anticipated rather than caught... it is already gone" (summary Callout), and "By the time you have consciously seen the alignment... it is gone" (end of The moment you cannot stage): The anticipation-not-reaction point is stated three times in near-identical words. -> Keep it in the decisive-moment section and the widget intro; cut the "anticipated rather than caught... already gone" sentence from the closing Callout.
- Widget hint on <Widget name="catch_the_moment">: The hint restates the preceding paragraph almost sentence for sentence (walker centered on an open stride, everything locked, spike not plateau, anticipate not react). -> Halve the hint to the operational bit only: "Scrub time, press when the walker is centered in the gap on an open stride. Everything else is locked; watch how narrow the scoring window is."
- Caption of 26.6 ("A wide lens used close builds the picture in depth... Step back or reach for a long lens and the layers flatten."): The first and last sentences re-teach 26.4's lesson; 26.4's caption already says "Step back or reach for a longer lens and you lose the room around him." Two captions make the same step-back argument. -> Cut the wide-close framing from 26.6 and spend the caption entirely on the three layers and the deep zone holding them, which is its distinct job in the Fishing/layering section.

## 27. landscape-and-architecture
_Verdict:_ A strong, well-imaged closer that hits the visually-led target with a genuinely distinct mix; the fixes needed are small: one miscounted prose claim, a self-contradicting caption on 27.4, and overlay lines on 27.4 that likely cross where the real edges do not.
**Top fixes (ranked):**
1. Fix the "all three of its subjects meet" sentence in the software-correction section: only geometry and resolution meet there, so say two, or genuinely bring in the third.
2. Reconcile 27.4's wording (drop "straight up" from caption and alt in favor of "tilted steeply up") and browser-verify overlay lines 2 and 3 so the paired edges converge without crossing inside the frame.
3. Cut the final sentences of captions 27.1 and 27.3; each restates the body paragraph sitting next to it and the book prizes tight captions.
4. Add one long-exposure photo (silky water or streaked cloud) to the dynamic-range section; the leaf-shutter/ND passage is the chapter's only visual claim with no image behind it.
**Correctness:**
- [MED][overlay][browser] 27.4: Overlay lines 2 and 3 cross each other near mid-frame (52,100 to 57,6 and 56,100 to 53,6), implying the two towers' facing edges intersect and a vanishing point inside the frame; in the photo the sky gap between the towers narrows toward the top but the edges never cross, and line 2's top endpoint (x=57) appears to land inside the right tower rather than on the left tower's edge. Lines 1 and 4 look correct.
- [MED][caption][static] 27.4: Caption says "The camera is tilted steeply up" then "Aimed straight up like this"; alt says "Looking straight up". The photo is a steep oblique look-up (tower bases visible at the frame bottom), so "straight up" is wrong in both places and the caption contradicts itself.
- [LOW][alt][static] 27.1: Alt says "under a clear blue sky" but a distinct band of cloud crosses the upper sky in the photo.
- [LOW][widget][static] level_tilt_correct: Hint says "the readout shows how many of the 100 megapixels the crop spends", but the widget displays what remains ("N MP after crop", "crop → N MP"), the inverse of what the hint promises.
**Image density:** Right count and right mix for a visually-led chapter: 5 image objects (1 Photo, 1 BeforeAfter, 3 AnnotatedPhoto) plus the Figure, Widget, and a LightroomPanel, and each photo teaches a distinct point (resolution depth, DR shadow lift, keystone problem, true-verticals goal, symmetry payoff). 27.6 and 27.7 sit closest together but earn their separation as goal vs compositional payoff; no cut needed. One real gap: the long-exposure passage has no image.
  - idea: Add a Photo of a long-exposure scene (silky waterfall or streaked cloud over a static landmark) with a caption tying it to the leaf shutter's minutes-long exposures and a strong ND. (where: Holding the whole range of light, after the paragraph ending "...only a static camera on a tripod can make."; why: That sentence is the only visual claim in the chapter with no image behind it, and the water-to-mist look is exactly the kind of thing a photograph teaches instantly and a paragraph cannot.)
**Clarity:**
- Fixing it after, and what it costs: "This is the one place in the chapter where all three of its subjects meet": The sentence then names only two subjects (geometry and resolution). Dynamic range never enters the correction story, so "all three" miscounts and sends the reader hunting for a third that is not there. -> Say "where two of the chapter's subjects collide: the keystone is a geometry problem, and the fix is a resolution cost paid from the 100-megapixel budget", or genuinely tie in the third subject.
- 27.4 caption: "The camera is tilted steeply up... Aimed straight up like this": The caption contradicts itself two sentences apart (steeply up vs straight up), and the alt says "Looking straight up" too. The photo is a steep oblique look-up with the tower bases visible, not a straight-up shot. -> Standardize on "tilted steeply up" in both the caption and the alt.
- "But acceptable is a circle-of-confusion standard": Circle of confusion lands unglossed and unlinked; it was defined chapters ago and this is the only load-bearing use here. -> Link the phrase to the aperture/depth-of-field chapter or add a six-word gloss like "the blur size a print forgives".
**Verbosity:**
- 27.1 caption, last sentence: "It only works if the entire frame is sharp, which is a matter of the right aperture and, often, more than one frame of focus.": Restates the two-fights setup (diffraction plus focus stacking) that the body paragraphs directly above and below already carry. -> Cut the sentence; end the caption on the print payoff.
- 27.3 caption, last sentence: "When even the file's range runs out, you bracket and blend, or hold the sky with a graduated filter.": Near-verbatim repeat of the bracket-and-blend / graduated-ND sentence in the body paragraph immediately above the component. -> Cut it from the caption.
- Figure 27.5 caption vs the paragraph that follows ("That leaves three honest ways..."): The caption walks all three cases in full and then the next paragraph re-explains the same three routes in more detail; the reader gets the enumeration twice back to back. -> Trim the caption to the sensor-plane rule plus the red-keystone / teal-sensor-line key, and let the body own the three routes.
